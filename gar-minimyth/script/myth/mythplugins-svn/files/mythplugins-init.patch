diff -Naur mythplugins-10355-old/mythdvd/mythdvd/settings.cpp mythplugins-10355-new/mythdvd/mythdvd/settings.cpp
--- mythplugins-10355-old/mythdvd/mythdvd/settings.cpp	2006-07-04 18:28:31.000000000 -0700
+++ mythplugins-10355-new/mythdvd/mythdvd/settings.cpp	2006-07-04 18:30:04.000000000 -0700
@@ -78,7 +78,7 @@
 {
     HostLineEdit *gc = new HostLineEdit("DVDPlayerCommand");
     gc->setLabel(QObject::tr("DVD Player Command"));
-    gc->setValue("mplayer dvd:// -dvd-device %d -fs -zoom -vo xv");
+    gc->setValue("xine -pfhq --no-splash dvd://");
     gc->setHelpText(QObject::tr("This can be any command to launch a DVD player "
                     "(e.g. MPlayer, ogle, etc.). If present, %d will "
                     "be substituted for the DVD device (e.g. /dev/dvd)."));
@@ -89,7 +89,7 @@
 {
     HostLineEdit *gc = new HostLineEdit("VCDPlayerCommand");
     gc->setLabel(QObject::tr("VCD Player Command"));
-    gc->setValue("mplayer vcd:// -cdrom-device %d -fs -zoom -vo xv");
+    gc->setValue("xine -pfhq --no-splash vcd://");
     gc->setHelpText(QObject::tr("This can be any command to launch a VCD player "
                     "(e.g. MPlayer, xine, etc.). If present, %d will "
                     "be substituted for the VCD device (e.g. /dev/cdrom)."));
diff -Naur mythplugins-10355-old/mythgallery/mythgallery/gallerysettings.cpp mythplugins-10355-new/mythgallery/mythgallery/gallerysettings.cpp
--- mythplugins-10355-old/mythgallery/mythgallery/gallerysettings.cpp	2006-07-04 18:28:31.000000000 -0700
+++ mythplugins-10355-new/mythgallery/mythgallery/gallerysettings.cpp	2006-07-04 18:30:04.000000000 -0700
@@ -15,7 +15,7 @@
 {
     HostLineEdit *gc = new HostLineEdit("GalleryDir");
     gc->setLabel(QObject::tr("Directory that holds images"));
-    gc->setValue("/var/lib/pictures");
+    gc->setValue("/mnt/gallery");
     gc->setHelpText(QObject::tr("This directory must exist and "
                        "MythGallery needs to have read permission."));
     return gc;
@@ -47,7 +47,7 @@
 {
     HostLineEdit *gc = new HostLineEdit("GalleryMoviePlayerCmd");
     gc->setLabel(QObject::tr("Command run to display movie files"));
-    gc->setValue("mplayer -fs %s");
+    gc->setValue("xine -pfhq --no-splash --loop");
     gc->setHelpText(QObject::tr("This command is executed whenever a movie "
                     "file is selected"));
     return gc;
@@ -67,7 +67,7 @@
 {
     HostLineEdit *gc = new HostLineEdit("GalleryImportDirs");
     gc->setLabel(QObject::tr("Paths to import images from"));
-    gc->setValue("/mnt/cdrom:/mnt/camera");
+    gc->setValue("");
     gc->setHelpText(QObject::tr("This is a colon separated list of paths. "
                     "If the path in the list is a directory, its contents will "
                     "be copied. If it is an executable, it will be run."));
diff -Naur mythplugins-10355-old/mythmusic/mythmusic/globalsettings.cpp mythplugins-10355-new/mythmusic/mythmusic/globalsettings.cpp
--- mythplugins-10355-old/mythmusic/mythmusic/globalsettings.cpp	2006-07-04 18:28:31.000000000 -0700
+++ mythplugins-10355-new/mythmusic/mythmusic/globalsettings.cpp	2006-07-04 18:30:04.000000000 -0700
@@ -19,7 +19,7 @@
 {
     HostLineEdit *gc = new HostLineEdit("MusicLocation");
     gc->setLabel(QObject::tr("Directory to hold music"));
-    gc->setValue("/mnt/store/music/");
+    gc->setValue("/mnt/music/");
     gc->setHelpText(QObject::tr("This directory must exist, and the user "
                     "running MythMusic needs to have write permission "
                     "to the directory."));
@@ -31,6 +31,10 @@
     HostComboBox *gc = new HostComboBox("MusicAudioDevice", true);
     gc->setLabel(QObject::tr("Audio device"));
     gc->addSelection(QObject::tr("default"), "default");
+
+    gc->addSelection(QObject::tr("ALSA:default"), "ALSA:default");
+    gc->addSelection(QObject::tr("ALSA:iec958"), "ALSA:iec958");
+
     QDir dev("/dev", "dsp*", QDir::Name, QDir::System);
     gc->fillSelectionsFromDir(dev);
     dev.setNameFilter("adsp*");
@@ -67,7 +71,7 @@
 {
     HostLineEdit *gc = new HostLineEdit("TreeLevels", true);
     gc->setLabel(QObject::tr("Tree Sorting"));
-    gc->setValue("splitartist artist album title");
+    gc->setValue("directory");
     gc->setHelpText(QObject::tr("Order in which to sort the Music "
                     "Tree. Possible values are a space-separated list of "
                     "genre, splitartist, splitartist1, artist, album, and "
@@ -90,7 +94,7 @@
 {
     HostLineEdit *gc = new HostLineEdit("NonID3FileNameFormat");
     gc->setLabel(QObject::tr("Filename Format"));
-    gc->setValue("GENRE/ARTIST/ALBUM/TRACK_TITLE");
+    gc->setValue("ARTIST/YEAR - ALBUM/TRACK - TITLE");
     gc->setHelpText(QObject::tr("Directory and filename Format used to grab "
                     "information if no ID3 information is found."));
     return gc;
@@ -191,7 +195,7 @@
 {
     HostLineEdit *gc = new HostLineEdit("FilenameTemplate");
     gc->setLabel(QObject::tr("File storage location"));
-    gc->setValue("ARTIST/ALBUM/TRACK-TITLE"); // Don't translate
+    gc->setValue("ARTIST/YEAR-ALBUM/TRACK-TITLE"); // Don't translate
     gc->setHelpText(QObject::tr("Defines the location/name for new songs. "
                     "Valid tokens are: GENRE, ARTIST, ALBUM, "
                     "TRACK, TITLE, YEAR, / and -. '-' will be replaced "
@@ -302,7 +306,7 @@
 {
     HostCheckBox *gc = new HostCheckBox("ListAsShuffled");
     gc->setLabel(QObject::tr("List as Shuffled"));
-    gc->setValue(false);
+    gc->setValue(true);
     gc->setHelpText(QObject::tr("List songs on the playback screen "
                     "in the order they will be played."));
     return gc;
@@ -312,7 +316,7 @@
 {
     HostCheckBox *gc = new HostCheckBox("ShowWholeTree");
     gc->setLabel(QObject::tr("Show entire music tree"));
-    gc->setValue(false);
+    gc->setValue(true);
     gc->setHelpText(QObject::tr("If selected, you can navigate your entire "
                     "music tree from the playing screen."));
     return gc;
@@ -380,7 +384,7 @@
 {
     HostLineEdit *gc = new HostLineEdit("VisualMode");
     gc->setLabel(QObject::tr("Visualizations"));
-    gc->setValue("Random");
+    gc->setValue("Blank");
     gc->setHelpText(QObject::tr("List of visualizations to use during playback. "
                     "Possible values are space-separated list of ") + "Random, "
                     "MonoScope, StereoScope, Spectrum, BumpScope, Goom, "
@@ -395,7 +399,7 @@
 {
     HostCheckBox *gc = new HostCheckBox("CDWriterEnabled");
     gc->setLabel(QObject::tr("Enable CD Writing."));
-    gc->setValue(true);
+    gc->setValue(false);
     gc->setHelpText(QObject::tr("Requires a SCSI or an IDE-SCSI CD Writer."));
     return gc;
 };
diff -Naur mythplugins-10355-old/mythvideo/mythvideo/globalsettings.cpp mythplugins-10355-new/mythvideo/mythvideo/globalsettings.cpp
--- mythplugins-10355-old/mythvideo/mythvideo/globalsettings.cpp	2006-07-04 18:28:31.000000000 -0700
+++ mythplugins-10355-new/mythvideo/mythvideo/globalsettings.cpp	2006-07-04 18:30:04.000000000 -0700
@@ -31,7 +31,7 @@
     gc->setLabel(QObject::tr("Default View"));
     gc->addSelection(QObject::tr("Gallery"), "1");
     gc->addSelection(QObject::tr("Browser"), "0");
-    gc->addSelection(QObject::tr("Listings"), "2"); 
+    gc->addSelection(QObject::tr("Listings"), "2", true); 
     gc->setHelpText(QObject::tr("The default view for MythVideo. "
                     "Other views can be reached via the popup menu available "
                     "via the MENU key."));
@@ -101,7 +101,7 @@
 {
     HostCheckBox *gc = new HostCheckBox("VideoTreeNoDB");
     gc->setLabel(QObject::tr("Video List browses files"));
-    gc->setValue(false);
+    gc->setValue(true);
     gc->setHelpText(QObject::tr("If set, this will cause the Video List "
                     "screen to show all relevant files below "
                     "the MythVideo starting directory whether "
@@ -172,7 +172,7 @@
 {
     HostLineEdit *gc = new HostLineEdit("VideoStartupDir");
     gc->setLabel(QObject::tr("Directory that holds videos"));
-    gc->setValue("/share/Movies/dvd");
+    gc->setValue("/mnt/video");
     gc->setHelpText(QObject::tr("This directory must exist, and the user "
                     "running MythVideo only needs to have read permission "
                     "to the directory."));
@@ -197,7 +197,7 @@
 {
     HostLineEdit *gc = new HostLineEdit("VideoDefaultPlayer");
     gc->setLabel(QObject::tr("Default Player"));
-    gc->setValue("mplayer -fs -zoom -quiet -vo xv %s");
+    gc->setValue("xine -pfhq --no-splash --loop");
     gc->setHelpText(QObject::tr("This is the command used for any file "
                     "that the extension is not specifically defined. "
                     "You may also enter the name of one of the playback "

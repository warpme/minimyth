commit c3beab6ddae1217ecbcdb4efb9306c61d807ea57
Author: Gwenole Beauchesne <gbeauchesne@splitted-desktop.com>
Date:   Tue Sep 22 13:03:23 2009 +0000

    Don't search LIBGL_DRIVERS_PATH, stick to LIBVA_DRIVERS_PATH only or the default path.

diff --git a/src/va.c b/src/va.c
index bc469a0..c07e10f 100644
--- a/src/va.c
+++ b/src/va.c
@@ -130,10 +130,6 @@ static VAStatus va_openDriver(VADisplay dpy, char *driver_name)
     {
         /* don't allow setuid apps to use LIBVA_DRIVERS_PATH */
         search_path = getenv("LIBVA_DRIVERS_PATH");
-        if (!search_path)
-        {
-            search_path = getenv("LIBGL_DRIVERS_PATH");
-        }
     }
     if (!search_path)
     {

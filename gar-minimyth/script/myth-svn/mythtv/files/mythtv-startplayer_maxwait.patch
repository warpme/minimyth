diff -Naur mythtv-15336-old/libs/libmythtv/tv_play.cpp mythtv-15336-new/libs/libmythtv/tv_play.cpp
--- mythtv-15336-old/libs/libmythtv/tv_play.cpp	2008-01-04 13:03:47.000000000 -0800
+++ mythtv-15336-new/libs/libmythtv/tv_play.cpp	2008-01-04 13:05:32.000000000 -0800
@@ -1390,7 +1390,7 @@
     SetupPlayer(isWatchingRecording);
     pthread_create(&decode, NULL, SpawnDecode, nvp);
 
-    maxWait = (maxWait <= 0) ? 20000 : maxWait;
+    maxWait = (maxWait <= 0) ? 40000 : maxWait;
 #ifdef USING_VALGRIND
     maxWait = (1<<30);
 #endif // USING_VALGRIND

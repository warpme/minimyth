diff -Naur alsa-driver-1.0.13-old/Makefile alsa-driver-1.0.13-new/Makefile
--- alsa-driver-1.0.13-old/Makefile	2006-09-29 04:40:28.000000000 -0700
+++ alsa-driver-1.0.13-new/Makefile	2006-09-30 17:13:18.000000000 -0700
@@ -194,9 +194,9 @@
 endif
 	@for d in $(SUBDIRS); do if ! $(MAKE) -C $$d modules_install; then exit 1; fi; done
 ifeq ($(DESTDIR),)
-	-/sbin/depmod -a $(kaversion) $(SYSTEM_MAP_OPT)
+	depmod -a $(kaversion) $(SYSTEM_MAP_OPT)
 else
-	-/sbin/depmod -a -b $(DESTDIR)/ $(SYSTEM_MAP_OPT) $(kaversion)
+	depmod -a -b $(DESTDIR)/ $(SYSTEM_MAP_OPT) $(kaversion)
 endif
 
 .PHONY: install-scripts

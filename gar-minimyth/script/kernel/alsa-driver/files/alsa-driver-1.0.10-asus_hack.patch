diff -Naur alsa-driver-1.0.10-old/alsa-kernel/pci/hda/hda_codec.c alsa-driver-1.0.10-new/alsa-kernel/pci/hda/hda_codec.c
--- alsa-driver-1.0.10-old/alsa-kernel/pci/hda/hda_codec.c	2005-11-07 06:38:44.000000000 -0800
+++ alsa-driver-1.0.10-new/alsa-kernel/pci/hda/hda_codec.c	2005-12-25 10:01:02.000000000 -0800
@@ -1772,6 +1772,7 @@
 	if (mout->hp_nid)
 		/* headphone out will just decode front left/right (stereo) */
 		snd_hda_codec_setup_stream(codec, mout->hp_nid, stream_tag, 0, format);
+#if 0
 	/* surrounds */
 	for (i = 1; i < mout->num_dacs; i++) {
 		if (chs >= (i + 1) * 2) /* independent out */
@@ -1781,6 +1782,7 @@
 			snd_hda_codec_setup_stream(codec, nids[i], stream_tag, 0,
 						   format);
 	}
+#endif
 	return 0;
 }
 
@@ -1792,10 +1794,14 @@
 	hda_nid_t *nids = mout->dac_nids;
 	int i;
 
+#if 0
 	for (i = 0; i < mout->num_dacs; i++)
 		snd_hda_codec_setup_stream(codec, nids[i], 0, 0, 0);
+#endif
 	if (mout->hp_nid)
 		snd_hda_codec_setup_stream(codec, mout->hp_nid, 0, 0, 0);
+	snd_hda_codec_setup_stream(codec, HDA_FRONT, 0, 0, 0);
+
 	down(&codec->spdif_mutex);
 	if (mout->dig_out_nid && mout->dig_out_used == HDA_DIG_ANALOG_DUP) {
 		snd_hda_codec_setup_stream(codec, mout->dig_out_nid, 0, 0, 0);

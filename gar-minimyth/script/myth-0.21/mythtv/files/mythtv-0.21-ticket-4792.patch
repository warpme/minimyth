diff -Naur mythtv-0.21-16949-old/libs/libmythtv/videoout_xv.cpp mythtv-0.21-16949-new/libs/libmythtv/videoout_xv.cpp
--- mythtv-0.21-16949-old/libs/libmythtv/videoout_xv.cpp	2008-04-04 18:48:37.000000000 -0700
+++ mythtv-0.21-16949-new/libs/libmythtv/videoout_xv.cpp	2008-04-04 18:51:47.000000000 -0700
@@ -1521,7 +1521,12 @@
         for (; it != renderers.end(); ++it)
             tmp += *it + ",";
 
-        renderer = renderers[0];
+        if ((db_vdisp_profile->GetVideoRenderer() == "xvmc-blit") && (renderers.contains("xv-blit")))
+            renderer = "xv-blit";
+        else if ((db_vdisp_profile->GetVideoRenderer() == "xvmc-opengl") && (renderers.contains("opengl")))
+            renderer = "opengl";
+        else
+            renderer = renderers[0];
         VERBOSE(VB_IMPORTANT, LOC + QString(
                     "Desired video renderer '%1' not available.\n\t\t\t"
                     "codec '%2' makes '%3' available, using '%4' instead.")

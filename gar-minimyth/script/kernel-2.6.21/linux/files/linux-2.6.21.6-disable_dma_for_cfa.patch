diff -Naur linux-2.6.21.6-old/drivers/ata/libata-core.c linux-2.6.21.6-new/drivers/ata/libata-core.c
--- linux-2.6.21.6-old/drivers/ata/libata-core.c	2007-07-06 21:47:55.000000000 -0700
+++ linux-2.6.21.6-new/drivers/ata/libata-core.c	2007-08-12 08:38:57.000000000 -0700
@@ -3482,6 +3482,12 @@
 			       "other device, disabling DMA\n");
 	}
 
+	if (ata_id_is_cfa(dev->id)) {
+		xfer_mask &= ~(ATA_MASK_MWDMA | ATA_MASK_UDMA);
+		ata_dev_printk(dev, KERN_WARNING,
+			       "device is a CFA, disabling DMA\n");
+	}
+
 	if (ap->ops->mode_filter)
 		xfer_mask = ap->ops->mode_filter(ap, dev, xfer_mask);
 
diff -Naur linux-2.6.21.6-old/drivers/ide/ide-dma.c linux-2.6.21.6-new/drivers/ide/ide-dma.c
--- linux-2.6.21.6-old/drivers/ide/ide-dma.c	2007-07-06 21:47:55.000000000 -0700
+++ linux-2.6.21.6-new/drivers/ide/ide-dma.c	2007-08-12 08:37:16.000000000 -0700
@@ -657,6 +657,12 @@
 				    drive->name, id->model);
 		return blacklist;
 	}
+	if (id->config == 0x848a) {
+		printk(KERN_WARNING "%s: Disabling (U)DMA for %s (CFA)\n",
+				    drive->name, id->model);
+		return 1;
+	}
+
 	return 0;
 }
 

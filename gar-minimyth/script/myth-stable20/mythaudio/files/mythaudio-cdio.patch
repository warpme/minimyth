diff -Naur mythaudio-31-old/configure.in mythaudio-31-new/configure.in
--- mythaudio-31-old/configure.in	2007-04-26 14:52:01.000000000 -0700
+++ mythaudio-31-new/configure.in	2007-04-26 15:30:51.000000000 -0700
@@ -112,19 +112,29 @@
     AC_MSG_ERROR(required 'cdaudio' not found.)
 fi
 
-if test "x$enable_cddaparanoia" != "xno" ; then
-    enable_cddaparanoia="yes"
-    AC_CHECK_HEADER(cdda_paranoia.h,,[AC_CHECK_HEADER(cdda/cdda_paranoia.h,,enable_cddaparanoia="no",
+enable_paranoia="no"
+enable_cdio="no"
+if test "x$enable_paranoia" = "xno" ; then
+    enable_paranoia="yes"
+    enable_cdio="yes"
+    AC_CHECK_HEADER(cdio/cdda.h,,[enable_paranoia="no",enable_cdio="no"])
+    AC_CHECK_HEADER(cdio/paranoia.h,,[enable_paranoia="no",enable_cdio="no"])
+    AC_CHECK_LIB(cdio_cdda,cdio_cddap_open,,[enable_paranoia="no",enable_cdio="no"])
+    AC_CHECK_LIB(cdio_paranoia,cdio_paranoia_init,,[enable_paranoia="no",enable_cdio="no"])
+fi
+if test "x$enable_paranoia" = "xno" ; then
+    enable_paranoia="yes"
+    AC_CHECK_HEADER(cdda_paranoia.h,,[AC_CHECK_HEADER(cdda/cdda_paranoia.h,,enable_paranoia="no",
 [
 #include <cdda/cdda_interface.h>
 ])],
 [
 #include <cdda_interface.h>
 ])
-    AC_CHECK_LIB(cdda_interface,cdda_read,,enable_cddaparanoia="no")
-    AC_CHECK_LIB(cdda_paranoia,paranoia_init,,enable_cddaparanoia="no")
+    AC_CHECK_LIB(cdda_interface,cdda_read,,enable_paranoia="no")
+    AC_CHECK_LIB(cdda_paranoia,paranoia_init,,enable_paranoia="no")
 fi
-if test "x$enable_cddaparanoia" = "xno" ; then
+if test "x$enable_paranoia" = "xno" ; then
     AC_MSG_ERROR(required 'CDDA Paranoia' not found.)
 fi
 
@@ -142,6 +152,12 @@
 
 echo "PREFIX = $prefix" >> ./mythaudio/config.pro
 
+if test "x$enable_cdio" = "xyes" ; then
+    AC_DEFINE([HAVE_CDIO],1,[Define to 1 to use libcdio for cdda/paranoia.])
+    echo "LIBS += -lcdio_paranoia -lcdio_cdda" >> ./mythaudio/config.pro
+else
+    echo "LIBS += -lcdda_paranoia -lcdda_interface" >> ./mythaudio/config.pro
+fi
 if test "x$enable_opengl" = "xyes" ; then
     AC_DEFINE([OPENGL_SUPPORT],1,[Define to 1 to enable OpenGL support.])
     echo "CONFIG += opengl" >> ./mythaudio/config.pro
diff -Naur mythaudio-31-old/mythaudio/cddecoder.cpp mythaudio-31-new/mythaudio/cddecoder.cpp
--- mythaudio-31-old/mythaudio/cddecoder.cpp	2007-04-26 14:52:01.000000000 -0700
+++ mythaudio-31-new/mythaudio/cddecoder.cpp	2007-04-26 15:34:38.000000000 -0700
@@ -167,7 +167,11 @@
     setOutput(0);
 }
 
+#ifdef HAVE_CDIO
+static void paranoia_cb(long int inpos, paranoia_cb_mode_t function)
+#else
 static void paranoia_cb(long inpos, int function)
+#endif
 {       
     inpos = inpos; function = function;
 }
diff -Naur mythaudio-31-old/mythaudio/cddecoder.h mythaudio-31-new/mythaudio/cddecoder.h
--- mythaudio-31-old/mythaudio/cddecoder.h	2007-04-26 14:52:01.000000000 -0700
+++ mythaudio-31-new/mythaudio/cddecoder.h	2007-04-26 15:34:03.000000000 -0700
@@ -5,10 +5,21 @@
 
 #include <cdaudio.h>
 extern "C" {
-#include <cdda_interface.h>
-#include <cdda_paranoia.h>
+#ifdef HAVE_CDIO
+#  include <cdio/cdda.h>
+#  include <cdio/paranoia.h>
+#else
+#  include <cdda_interface.h>
+#  include <cdda_paranoia.h>
+#endif
 }
 
+#ifdef HAVE_CDIO
+#  define CD_FRAMESIZE_RAW CDIO_CD_FRAMESIZE_RAW
+#  define cdrom_drive cdrom_drive_t
+#  define cdrom_paranoia cdrom_paranoia_t
+#endif
+
 class Metadata;
 
 class CdDecoder : public Decoder
diff -Naur mythaudio-31-old/mythaudio/mythaudio.pro mythaudio-31-new/mythaudio/mythaudio.pro
--- mythaudio-31-old/mythaudio/mythaudio.pro	2007-04-26 14:52:01.000000000 -0700
+++ mythaudio-31-new/mythaudio/mythaudio.pro	2007-04-26 15:32:42.000000000 -0700
@@ -20,7 +20,7 @@
 INSTALLS += installfiles uifiles
 
 LIBS += -lmad -lid3tag -logg -lvorbisfile -lvorbis -lvorbisenc -lcdaudio -lFLAC
-LIBS += -lmp3lame -lcdda_paranoia -lcdda_interface
+LIBS += -lmp3lame
 
 # Input
 HEADERS += AudioPlayer.h

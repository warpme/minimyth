diff -Naur mythtv-trunk-26578-old/libs/libmythtv/vsync.cpp mythtv-trunk-26578-new/libs/libmythtv/vsync.cpp
--- mythtv-trunk-26578-old/libs/libmythtv/vsync.cpp	2010-09-29 15:44:07.000000000 -0700
+++ mythtv-trunk-26578-new/libs/libmythtv/vsync.cpp	2010-09-29 15:44:08.000000000 -0700
@@ -100,7 +100,11 @@
 //    TESTVIDEOSYNC(VDPAUVideoSync);
 #endif
 #ifndef _WIN32
-    TESTVIDEOSYNC(DRMVideoSync);
+    if (gCoreContext->GetNumSetting("UseDRMVSync", 1) &&
+       (getenv("NO_DRM_VSYNC") == NULL))
+    {
+        TESTVIDEOSYNC(DRMVideoSync);
+    }
 #ifdef USING_OPENGL_VSYNC
 /*
     if (gCoreContext->GetNumSetting("UseOpenGLVSync", 1) &&
diff -Naur mythtv-trunk-26578-old/programs/mythfrontend/globalsettings.cpp mythtv-trunk-26578-new/programs/mythfrontend/globalsettings.cpp
--- mythtv-trunk-26578-old/programs/mythfrontend/globalsettings.cpp	2010-09-29 15:44:07.000000000 -0700
+++ mythtv-trunk-26578-new/programs/mythfrontend/globalsettings.cpp	2010-09-29 15:44:08.000000000 -0700
@@ -3530,6 +3530,18 @@
     };
 };
 
+static HostCheckBox *UseDRMVSync()
+{
+    HostCheckBox *gc = new HostCheckBox("UseDRMVSync");
+    gc->setLabel(QObject::tr("Enable DRM vertical sync for timing"));
+    gc->setValue(false);
+    gc->setHelpText(QObject::tr(
+                        "If it is supported by your hardware/drivers, "
+                        "MythTV will use DRM vertical syncing for "
+                        "video timing, reducing frame jitter."));
+    return gc;
+}
+
 #ifdef USING_OPENGL_VSYNC
 /*
 static HostCheckBox *UseOpenGLVSync()
@@ -4057,6 +4069,7 @@
 
     general1->addChild(columns);
     general1->addChild(LiveTVIdleTimeout());
+    general1->addChild(UseDRMVSync());
 #ifdef USING_OPENGL_VSYNC
     //general1->addChild(UseOpenGLVSync());
 #endif // USING_OPENGL_VSYNC

diff -Naur mythtv-trunk-19435-old/libs/libmythtv/videoout_xv.cpp mythtv-trunk-19435-new/libs/libmythtv/videoout_xv.cpp
--- mythtv-trunk-19435-old/libs/libmythtv/videoout_xv.cpp	2008-12-22 20:38:51.000000000 -0800
+++ mythtv-trunk-19435-new/libs/libmythtv/videoout_xv.cpp	2008-12-22 20:38:52.000000000 -0800
@@ -1818,7 +1818,12 @@
         for (; it != renderers.end(); ++it)
             tmp += *it + ",";
 
-        renderer = renderers[0];
+        if ((db_vdisp_profile->GetVideoRenderer() == "xvmc-blit") && (renderers.contains("xv-blit")))
+            renderer = "xv-blit";
+        else if ((db_vdisp_profile->GetVideoRenderer() == "xvmc-opengl") && (renderers.contains("opengl")))
+            renderer = "opengl";
+        else
+            renderer = renderers[0];
         VERBOSE(VB_IMPORTANT, LOC + QString(
                     "Desired video renderer '%1' not available.\n\t\t\t"
                     "codec '%2' makes '%3' available, using '%4' instead.")

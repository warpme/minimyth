diff -Naur mythtv-trunk-16465-old/libs/libmythtv/videoout_xv.cpp mythtv-trunk-16465-new/libs/libmythtv/videoout_xv.cpp
--- mythtv-trunk-16465-old/libs/libmythtv/videoout_xv.cpp	2008-03-08 16:07:29.000000000 -0800
+++ mythtv-trunk-16465-new/libs/libmythtv/videoout_xv.cpp	2008-03-08 16:10:37.000000000 -0800
@@ -1521,7 +1521,10 @@
         for (; it != renderers.end(); ++it)
             tmp += *it + ",";
 
-        renderer = renderers[0];
+    	if ((db_vdisp_profile->GetVideoRenderer() == "xvmc-blit") && (renderers.contains("xv-blit")))
+            renderer = "xv-blit" ;
+        else
+            renderer = renderers[0];
         VERBOSE(VB_IMPORTANT, LOC + QString(
                     "Desired video renderer '%1' not available.\n\t\t\t"
                     "codec '%2' makes '%3' available, using '%4' instead.")

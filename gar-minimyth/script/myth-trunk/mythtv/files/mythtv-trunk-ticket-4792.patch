diff -Naur mythtv-trunk-18885-old/libs/libmythtv/videoout_xv.cpp mythtv-trunk-18885-new/libs/libmythtv/videoout_xv.cpp
--- mythtv-trunk-18885-old/libs/libmythtv/videoout_xv.cpp	2008-10-26 17:06:34.000000000 -0700
+++ mythtv-trunk-18885-new/libs/libmythtv/videoout_xv.cpp	2008-10-26 17:06:35.000000000 -0700
@@ -1553,7 +1553,12 @@
         for (; it != renderers.end(); ++it)
             tmp += *it + ",";
 
-        renderer = renderers[0];
+        if ((db_vdisp_profile->GetVideoRenderer() == "xvmc-blit") && (renderers.contains("xv-blit")))
+            renderer = "xv-blit";
+        else if ((db_vdisp_profile->GetVideoRenderer() == "xvmc-opengl") && (renderers.contains("opengl")))
+            renderer = "opengl";
+        else
+            renderer = renderers[0];
         VERBOSE(VB_IMPORTANT, LOC + QString(
                     "Desired video renderer '%1' not available.\n\t\t\t"
                     "codec '%2' makes '%3' available, using '%4' instead.")

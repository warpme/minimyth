diff -Naur mythtv-trunk-19356-old/libs/libmythtv/videoout_xv.cpp mythtv-trunk-19356-new/libs/libmythtv/videoout_xv.cpp
--- mythtv-trunk-19356-old/libs/libmythtv/videoout_xv.cpp	2008-12-13 08:02:40.000000000 -0800
+++ mythtv-trunk-19356-new/libs/libmythtv/videoout_xv.cpp	2008-12-13 08:02:41.000000000 -0800
@@ -1647,7 +1647,12 @@
         for (; it != renderers.end(); ++it)
             tmp += *it + ",";
 
-        renderer = renderers[0];
+        if ((db_vdisp_profile->GetVideoRenderer() == "xvmc-blit") && (renderers.contains("xv-blit")))
+            renderer = "xv-blit";
+        else if ((db_vdisp_profile->GetVideoRenderer() == "xvmc-opengl") && (renderers.contains("opengl")))
+            renderer = "opengl";
+        else
+            renderer = renderers[0];
         VERBOSE(VB_IMPORTANT, LOC + QString(
                     "Desired video renderer '%1' not available.\n\t\t\t"
                     "codec '%2' makes '%3' available, using '%4' instead.")

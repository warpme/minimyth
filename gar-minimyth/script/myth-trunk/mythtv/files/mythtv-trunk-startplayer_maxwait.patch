diff -Naur mythtv-trunk-16581-old/libs/libmythtv/tv_play.cpp mythtv-trunk-16581-new/libs/libmythtv/tv_play.cpp
--- mythtv-trunk-16581-old/libs/libmythtv/tv_play.cpp	2008-03-15 16:05:43.000000000 -0700
+++ mythtv-trunk-16581-new/libs/libmythtv/tv_play.cpp	2008-03-15 16:05:44.000000000 -0700
@@ -1816,7 +1816,7 @@
     SetupPlayer(isWatchingRecording);
     pthread_create(&decode, NULL, SpawnDecode, nvp);
 
-    maxWait = (maxWait <= 0) ? 20000 : maxWait;
+    maxWait = (maxWait <= 0) ? 40000 : maxWait;
 #ifdef USING_VALGRIND
     maxWait = (1<<30);
 #endif // USING_VALGRIND

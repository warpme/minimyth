diff -Naur mythtv-trunk-18866-old/libs/libmythtv/tv_play.cpp mythtv-trunk-18866-new/libs/libmythtv/tv_play.cpp
--- mythtv-trunk-18866-old/libs/libmythtv/tv_play.cpp	2008-10-24 17:01:16.000000000 -0700
+++ mythtv-trunk-18866-new/libs/libmythtv/tv_play.cpp	2008-10-24 17:01:17.000000000 -0700
@@ -1816,7 +1816,7 @@
     if (pthread_create(&decode, NULL, SpawnDecode, nvp))
         return false;
 
-    maxWait = (maxWait <= 0) ? 20000 : maxWait;
+    maxWait = (maxWait <= 0) ? 40000 : maxWait;
 #ifdef USING_VALGRIND
     maxWait = (1<<30);
 #endif // USING_VALGRIND

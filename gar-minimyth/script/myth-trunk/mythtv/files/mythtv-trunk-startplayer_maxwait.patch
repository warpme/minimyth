diff -Naur mythtv-trunk-17324-old/libs/libmythtv/tv_play.cpp mythtv-trunk-17324-new/libs/libmythtv/tv_play.cpp
--- mythtv-trunk-17324-old/libs/libmythtv/tv_play.cpp	2008-05-14 06:50:25.000000000 -0700
+++ mythtv-trunk-17324-new/libs/libmythtv/tv_play.cpp	2008-05-14 06:50:26.000000000 -0700
@@ -1784,7 +1784,7 @@
     SetupPlayer(isWatchingRecording);
     pthread_create(&decode, NULL, SpawnDecode, nvp);
 
-    maxWait = (maxWait <= 0) ? 20000 : maxWait;
+    maxWait = (maxWait <= 0) ? 40000 : maxWait;
 #ifdef USING_VALGRIND
     maxWait = (1<<30);
 #endif // USING_VALGRIND

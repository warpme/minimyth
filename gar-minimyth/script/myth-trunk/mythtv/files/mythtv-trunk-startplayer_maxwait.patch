diff -Naur mythtv-trunk-17598-old/libs/libmythtv/tv_play.cpp mythtv-trunk-17598-new/libs/libmythtv/tv_play.cpp
--- mythtv-trunk-17598-old/libs/libmythtv/tv_play.cpp	2008-06-23 16:21:21.000000000 -0700
+++ mythtv-trunk-17598-new/libs/libmythtv/tv_play.cpp	2008-06-23 16:21:22.000000000 -0700
@@ -1787,7 +1787,7 @@
     SetupPlayer(isWatchingRecording);
     pthread_create(&decode, NULL, SpawnDecode, nvp);
 
-    maxWait = (maxWait <= 0) ? 20000 : maxWait;
+    maxWait = (maxWait <= 0) ? 40000 : maxWait;
 #ifdef USING_VALGRIND
     maxWait = (1<<30);
 #endif // USING_VALGRIND

diff -Naur mythtv-trunk-19211-old/libs/libmythtv/tv_play.cpp mythtv-trunk-19211-new/libs/libmythtv/tv_play.cpp
--- mythtv-trunk-19211-old/libs/libmythtv/tv_play.cpp	2008-12-02 04:54:32.000000000 -0800
+++ mythtv-trunk-19211-new/libs/libmythtv/tv_play.cpp	2008-12-02 04:54:33.000000000 -0800
@@ -1817,7 +1817,7 @@
     if (pthread_create(&decode, NULL, SpawnDecode, nvp))
         return false;
 
-    maxWait = (maxWait <= 0) ? 20000 : maxWait;
+    maxWait = (maxWait <= 0) ? 40000 : maxWait;
 #ifdef USING_VALGRIND
     maxWait = (1<<30);
 #endif // USING_VALGRIND

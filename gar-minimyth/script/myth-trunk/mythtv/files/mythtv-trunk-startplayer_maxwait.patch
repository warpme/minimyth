diff -Naur mythtv-trunk-16989-old/libs/libmythtv/tv_play.cpp mythtv-trunk-16989-new/libs/libmythtv/tv_play.cpp
--- mythtv-trunk-16989-old/libs/libmythtv/tv_play.cpp	2008-04-08 14:20:52.000000000 -0700
+++ mythtv-trunk-16989-new/libs/libmythtv/tv_play.cpp	2008-04-08 14:20:55.000000000 -0700
@@ -1830,7 +1830,7 @@
     SetupPlayer(isWatchingRecording);
     pthread_create(&decode, NULL, SpawnDecode, nvp);
 
-    maxWait = (maxWait <= 0) ? 20000 : maxWait;
+    maxWait = (maxWait <= 0) ? 40000 : maxWait;
 #ifdef USING_VALGRIND
     maxWait = (1<<30);
 #endif // USING_VALGRIND

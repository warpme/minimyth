diff -Naur mythtv-trunk-18515-old/libs/libmythtv/tv_play.cpp mythtv-trunk-18515-new/libs/libmythtv/tv_play.cpp
--- mythtv-trunk-18515-old/libs/libmythtv/tv_play.cpp	2008-09-29 16:35:26.000000000 -0700
+++ mythtv-trunk-18515-new/libs/libmythtv/tv_play.cpp	2008-09-29 16:35:27.000000000 -0700
@@ -1806,7 +1806,7 @@
     if (pthread_create(&decode, NULL, SpawnDecode, nvp))
         return false;
 
-    maxWait = (maxWait <= 0) ? 20000 : maxWait;
+    maxWait = (maxWait <= 0) ? 40000 : maxWait;
 #ifdef USING_VALGRIND
     maxWait = (1<<30);
 #endif // USING_VALGRIND

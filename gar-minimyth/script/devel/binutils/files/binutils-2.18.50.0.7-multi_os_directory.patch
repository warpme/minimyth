diff -Naur binutils-2.18.50.0.7-old/bfd/configure binutils-2.18.50.0.7-new/bfd/configure
--- binutils-2.18.50.0.7-old/bfd/configure	2008-05-04 07:28:09.000000000 -0700
+++ binutils-2.18.50.0.7-new/bfd/configure	2008-06-14 05:57:11.000000000 -0700
@@ -8729,7 +8729,7 @@
   # Ok, now we have the path, separated by spaces, we can step through it
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
-  lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  lt_multi_os_dir='../lib'
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
diff -Naur binutils-2.18.50.0.7-old/binutils/configure binutils-2.18.50.0.7-new/binutils/configure
--- binutils-2.18.50.0.7-old/binutils/configure	2008-05-04 07:28:09.000000000 -0700
+++ binutils-2.18.50.0.7-new/binutils/configure	2008-06-14 05:57:15.000000000 -0700
@@ -8559,7 +8559,7 @@
   # Ok, now we have the path, separated by spaces, we can step through it
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
-  lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  lt_multi_os_dir='../lib'
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
diff -Naur binutils-2.18.50.0.7-old/gas/configure binutils-2.18.50.0.7-new/gas/configure
--- binutils-2.18.50.0.7-old/gas/configure	2008-05-04 07:28:10.000000000 -0700
+++ binutils-2.18.50.0.7-new/gas/configure	2008-06-14 05:57:27.000000000 -0700
@@ -8557,7 +8557,7 @@
   # Ok, now we have the path, separated by spaces, we can step through it
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
-  lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  lt_multi_os_dir='../lib'
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
diff -Naur binutils-2.18.50.0.7-old/gprof/configure binutils-2.18.50.0.7-new/gprof/configure
--- binutils-2.18.50.0.7-old/gprof/configure	2008-05-04 07:28:10.000000000 -0700
+++ binutils-2.18.50.0.7-new/gprof/configure	2008-06-14 05:57:34.000000000 -0700
@@ -8649,7 +8649,7 @@
   # Ok, now we have the path, separated by spaces, we can step through it
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
-  lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  lt_multi_os_dir='../lib'
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
diff -Naur binutils-2.18.50.0.7-old/ld/configure binutils-2.18.50.0.7-new/ld/configure
--- binutils-2.18.50.0.7-old/ld/configure	2008-05-04 07:28:10.000000000 -0700
+++ binutils-2.18.50.0.7-new/ld/configure	2008-06-14 05:57:42.000000000 -0700
@@ -8803,7 +8803,7 @@
   # Ok, now we have the path, separated by spaces, we can step through it
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
-  lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  lt_multi_os_dir='../lib'
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
diff -Naur binutils-2.18.50.0.7-old/libiberty/Makefile.in binutils-2.18.50.0.7-new/libiberty/Makefile.in
--- binutils-2.18.50.0.7-old/libiberty/Makefile.in	2008-04-03 09:54:05.000000000 -0700
+++ binutils-2.18.50.0.7-new/libiberty/Makefile.in	2008-06-14 05:54:21.000000000 -0700
@@ -352,7 +352,7 @@
 # multilib-specific flags, it's overridden by FLAGS_TO_PASS from the
 # default multilib, so we have to take LIBCFLAGS into account as well,
 # since it will be passed the multilib flags.
-MULTIOSDIR = `$(CC) $(LIBCFLAGS) -print-multi-os-directory`
+MULTIOSDIR = '../lib'
 install_to_libdir: all
 	${mkinstalldirs} $(DESTDIR)$(libdir)/$(MULTIOSDIR)
 	$(INSTALL_DATA) $(TARGETLIB) $(DESTDIR)$(libdir)/$(MULTIOSDIR)/$(TARGETLIB)n
diff -Naur binutils-2.18.50.0.7-old/libtool.m4 binutils-2.18.50.0.7-new/libtool.m4
--- binutils-2.18.50.0.7-old/libtool.m4	2008-04-03 09:54:04.000000000 -0700
+++ binutils-2.18.50.0.7-new/libtool.m4	2008-06-14 05:58:22.000000000 -0700
@@ -1929,7 +1929,7 @@
   # Ok, now we have the path, separated by spaces, we can step through it
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
-  lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  lt_multi_os_dir='../lib'
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
diff -Naur binutils-2.18.50.0.7-old/opcodes/configure binutils-2.18.50.0.7-new/opcodes/configure
--- binutils-2.18.50.0.7-old/opcodes/configure	2008-05-04 07:28:10.000000000 -0700
+++ binutils-2.18.50.0.7-new/opcodes/configure	2008-06-14 05:57:52.000000000 -0700
@@ -8725,7 +8725,7 @@
   # Ok, now we have the path, separated by spaces, we can step through it
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
-  lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  lt_multi_os_dir='../lib'
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"

diff -Naur xf86-video-intel-2.18.0-old/configure.ac xf86-video-intel-2.18.0-new/configure.ac
--- xf86-video-intel-2.18.0-old/configure.ac	2012-02-24 03:26:14.000000000 -0800
+++ xf86-video-intel-2.18.0-new/configure.ac	2012-02-29 13:01:37.000000000 -0800
@@ -53,6 +53,8 @@
 AC_DISABLE_STATIC
 AC_PROG_LIBTOOL
 
+PKG_PROG_PKG_CONFIG
+
 # Are we in a git checkout?
 dot_git=no
 if test -e .git; then
@@ -187,7 +189,8 @@
 PKG_CHECK_MODULES(DRI2, [dri2proto >= 2.6],, DRI2=no)
 PKG_CHECK_MODULES(PCIACCESS, [pciaccess >= 0.10])
 
-sdkdir=`$PKG_CONFIG --variable=sdkdir xorg-server`
+m4_pattern_allow([^PKG_CONFIG_SYSROOT_DIR$])
+sdkdir="$PKG_CONFIG_SYSROOT_DIR$($PKG_CONFIG --variable=sdkdir xorg-server)"
 
 if test "x$enable_dri" != "xno"; then
         save_CFLAGS="$CFLAGS"

#!/usr/bin/perl

use strict;
use warnings;

require MiniMyth;

my $action = shift;

my $minimyth = new MiniMyth;

my $command = $minimyth->var_get('MM_EXTERNAL_' . "\U$action");
if ((defined($command)) && ($command))
{
    system(qq($command));
}

1;

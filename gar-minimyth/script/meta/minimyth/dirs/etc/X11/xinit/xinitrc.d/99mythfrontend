################################################################################
# Start mythfrontend.
#
# mythfrontend (not the window manager) is the applications that runs in the
# foreground because it is the only application that we can be sure will be
# running.
################################################################################

if /usr/bin/test -z "${MM_DEBUG}" ; then
    /usr/bin/mythfrontend
else
    /usr/bin/mythfrontend -v all
fi

#!/bin/sh

. /etc/minimyth.d/minimyth.functions

[ -n "${MMDEBUG}" ] && set -x

# Bring up the local loopback
ifconfig lo 127.0.0.1
# Get IP address and other IP parameters
mkdir -p /tmp/etc
mkdir -p /tmp/etc/minimyth.d
udhcpc -s /etc/minimyth.d/dhcp.script

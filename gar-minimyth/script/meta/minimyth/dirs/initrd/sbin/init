#!/bin/sh
mount -n -t tmpfs                                       none /rootfs-rw
mount -n -t unionfs -o dirs=/rootfs-rw=rw:/rootfs-ro=ro none /rootfs
cd /rootfs
mkdir -p initrd
pivot_root . initrd
exec /usr/sbin/chroot . ./bin/sh -c 'exec /sbin/init' <dev/console >dev/console 2>&1

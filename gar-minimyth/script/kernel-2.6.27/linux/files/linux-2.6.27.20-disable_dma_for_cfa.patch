diff -Naur linux-2.6.27.20-old/drivers/ata/libata-core.c linux-2.6.27.20-new/drivers/ata/libata-core.c
--- linux-2.6.27.20-old/drivers/ata/libata-core.c	2009-03-16 20:48:57.000000000 -0700
+++ linux-2.6.27.20-new/drivers/ata/libata-core.c	2009-03-16 20:51:39.000000000 -0700
@@ -4248,6 +4248,12 @@
 			       "other device, disabling DMA\n");
 	}
 
+	if (ata_id_is_cfa(dev->id)) {
+		xfer_mask &= ~(ATA_MASK_MWDMA | ATA_MASK_UDMA);
+		ata_dev_printk(dev, KERN_WARNING,
+			       "device is a CFA, disabling DMA\n");
+	}
+
 	if (ap->flags & ATA_FLAG_NO_IORDY)
 		xfer_mask &= ata_pio_mask_no_iordy(dev);
 
diff -Naur linux-2.6.27.20-old/drivers/ide/ide-dma.c linux-2.6.27.20-new/drivers/ide/ide-dma.c
--- linux-2.6.27.20-old/drivers/ide/ide-dma.c	2009-03-16 20:48:57.000000000 -0700
+++ linux-2.6.27.20-new/drivers/ide/ide-dma.c	2009-03-16 20:51:39.000000000 -0700
@@ -599,6 +599,12 @@
 				    drive->name, id->model);
 		return blacklist;
 	}
+	if (id->config == 0x848a) {
+		printk(KERN_WARNING "%s: Disabling (U)DMA for %s (CFA)\n",
+				    drive->name, id->model);
+		return 1;
+	}
+
 	return 0;
 }
 

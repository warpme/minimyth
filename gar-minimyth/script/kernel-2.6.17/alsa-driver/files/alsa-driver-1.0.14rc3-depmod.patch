diff -Naur alsa-driver-1.0.14rc3-old/Makefile alsa-driver-1.0.14rc3-new/Makefile
--- alsa-driver-1.0.14rc3-old/Makefile	2007-03-06 04:26:24.000000000 -0800
+++ alsa-driver-1.0.14rc3-new/Makefile	2007-03-06 16:55:57.000000000 -0800
@@ -198,7 +198,7 @@
 endif
 	@for d in $(SUBDIRS); do if ! $(MAKE) -C $$d modules_install; then exit 1; fi; done
 ifeq ($(DESTDIR),)
-	-/sbin/depmod -a $(kaversion) $(SYSTEM_MAP_OPT)
+	-depmod -a $(kaversion) $(SYSTEM_MAP_OPT)
 else
 	@echo "*** This is a \`staged' install using the prefix"
 	@echo "***	\"$(DESTDIR)\"."

diff -Naur unionfs-1.3-old/Makefile unionfs-1.3-new/Makefile
--- unionfs-1.3-old/Makefile	2006-06-26 12:46:44.000000000 -0700
+++ unionfs-1.3-new/Makefile	2007-05-01 10:58:42.000000000 -0700
@@ -8,6 +8,7 @@
 # This is where the make install target will put stuff
 PREFIX   = /usr/local
 MANDIR   = ${PREFIX}/man
+SBINDIR  = ${PREFIX}/sbin
 MODPREFIX=
 VERSION  = 1.3
 SUP_MAJOR= 2
@@ -33,8 +34,8 @@
 
 CC	= gcc
 KERNELVERSION=$(shell echo $(KVERS) | cut -d. -f1,2)
-EXTRA_CFLAGS += ${TOPINC} -Wall -Werror ${EXTRACFLAGS} ${UNIONFS_DEBUG_CFLAG} ${UNIONFS_OPT_CFLAG} ${UNIONFS_VERSION_CFLAG}
-UCFLAGS = -I. ${UNIONFS_DEBUG_CFLAG} ${UNIONFS_OPT_CFLAG} -Wall -Werror ${EXTRAUCFLAGS}  ${UNIONFS_VERSION_CFLAG}
+EXTRA_CFLAGS += ${TOPINC} -Wall ${EXTRACFLAGS} ${UNIONFS_DEBUG_CFLAG} ${UNIONFS_OPT_CFLAG} ${UNIONFS_VERSION_CFLAG}
+UCFLAGS = -I. ${UNIONFS_DEBUG_CFLAG} ${UNIONFS_OPT_CFLAG} -Wall ${EXTRAUCFLAGS}  ${UNIONFS_VERSION_CFLAG}
 
 obj-m := unionfs.o
 unionfs-objs := subr.o dentry.o file.o inode.o main.o super.o \
@@ -51,7 +52,7 @@
 	@echo ""
 
 unionfs.ko: FRC
-	make -C ${LINUXSRC} SUBDIRS=$(PWD) FISTDEVMK=$(PWD)/fistdev.mk modules
+	make -C ${LINUXSRC} SUBDIRS=`pwd` FISTDEVMK=`pwd`/fistdev.mk modules
 
 FRC:
 
@@ -68,7 +69,7 @@
 	ctags $^
 
 clean:
-	-make -C ${LINUXSRC} SUBDIRS=$(PWD) FISTDEVMK=$(PWD)/fistdev.mk clean
+	-make -C ${LINUXSRC} SUBDIRS=`pwd` FISTDEVMK=`pwd`/fistdev.mk clean
 	rm -f ${unionfs-objs} ${obj-m} ${obj-m:.o=.ko} *.d .*.flags \#* *~
 	rm -f TAGS tags
 	rm -f unionfs-${VERSION}.tar${COMPEXT}
@@ -98,13 +99,11 @@
 install-mod: unionfs.ko
 	mkdir -p ${MODPREFIX}/${MODDIR}/kernel/fs/unionfs
 	cp unionfs.ko ${MODPREFIX}/${MODDIR}/kernel/fs/unionfs
-	-/sbin/depmod -a
 
 install: install-utils install-mod
 
 uninstall:
 	rm ${MODPREFIX}/${MODDIR}/kernel/fs/unionfs.ko
-	-/sbin/depmod -a
 	rm ${MANDIR}/man8/unionctl.8
 	rm ${MANDIR}/man8/uniondbg.8
 	rm ${MANDIR}/man4/unionfs.4
diff -Naur unionfs-1.3-old/utils/Makefile unionfs-1.3-new/utils/Makefile
--- unionfs-1.3-old/utils/Makefile	2006-06-26 12:46:45.000000000 -0700
+++ unionfs-1.3-new/utils/Makefile	2007-05-01 10:58:26.000000000 -0700
@@ -24,12 +24,12 @@
 PHONY. utils: ${BINS}
 
 install: utils
-	mkdir -p ${PREFIX}/sbin
-	cp unionctl ${PREFIX}/sbin
-	cp uniondbg ${PREFIX}/sbin
-	cp unionimap ${PREFIX}/sbin
+	mkdir -p ${SBINDIR}
+	cp unionctl ${SBINDIR}
+	cp uniondbg ${SBINDIR}
+	cp unionimap ${SBINDIR}
 
 uninstall:
-	rm ${PREFIX}/sbin/unionctl
-	rm ${PREFIX}/sbin/uniondbg
-	rm ${PREFIX}/sbin/unionimap
+	rm ${SBINDIR}/unionctl
+	rm ${SBINDIR}/uniondbg
+	rm ${SBINDIR}/unionimap

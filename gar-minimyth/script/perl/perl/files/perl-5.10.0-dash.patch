diff -Naur perl-5.10.0-old/Configure perl-5.10.0-new/Configure
--- perl-5.10.0-old/Configure	2007-12-18 02:47:07.000000000 -0800
+++ perl-5.10.0-new/Configure	2008-04-23 07:03:22.000000000 -0700
@@ -1892,7 +1892,7 @@
 	*) exec 1>&4;;
 	esac
 	case "$config_sh" in
-	'') config_sh='config.sh';;
+	'') config_sh='./config.sh';;
 	esac
 	echo " "
 	echo "Fetching answers from $config_sh..."
@@ -1900,7 +1900,7 @@
 	. $config_sh
 	test "$override" && . ./optdef.sh
 	echo " "
-	. UU/extract
+	. ./UU/extract
 	rm -rf UU
 	echo "Extraction done."
 	exit 0
@@ -3065,7 +3065,7 @@
 if test -f config.sh; then
 	echo " "
 	rp="I see a config.sh file.  Shall I use it to set the defaults?"
-	. UU/myread
+	. ./UU/myread
 	case "$ans" in
 	n*|N*) echo "OK, I'll ignore it."
 		mv config.sh config.sh.old
@@ -3454,11 +3454,11 @@
 EOM
 
 	rp="Which of these apply, if any?"
-	. UU/myread
+	. ./UU/myread
 	tans=$ans
 	for file in $tans; do
 		if $test X$file = XPolicy -a -f Policy.sh; then
-			. Policy.sh
+			. ./Policy.sh
 			$cat Policy.sh >> UU/config.sh
 		elif $test -f $src/hints/$file.sh; then
 			. $src/hints/$file.sh
@@ -3470,7 +3470,7 @@
 			echo "$file.sh does not exist"
 			dflt=$file
 			rp="hint to use instead?"
-			. UU/myread
+			. ./UU/myread
 			for file in $ans; do
 				if $test -f "$src/hints/$file.sh"; then
 					. $src/hints/$file.sh
@@ -3706,7 +3706,7 @@
 	case "$tilde" in
 	true) rp="$rp (~name ok)";;
 	esac
-	. UU/myread
+	. ./UU/myread
 	if test -f UU/getfile.ok && \
 		$contains "^$ans\$" UU/getfile.ok >/dev/null 2>&1
 	then
@@ -3840,7 +3840,7 @@
 					dflt=n
 				fi
 				rp="$what $value doesn't exist.  Use that name anyway?"
-				. UU/myread
+				. ./UU/myread
 				dflt=''
 				case "$ans" in
 				y*) type='';;
@@ -21457,7 +21457,7 @@
 	echo " "
 	dflt=y
 	rp='I see a config.over file.  Do you wish to load it?'
-	. UU/myread
+	. ./UU/myread
 	case "$ans" in
 	n*) echo "OK, I'll ignore it.";;
 	*)	. ./config.over
@@ -22585,7 +22585,7 @@
 
 EOM
 	rp="Press return or use a shell escape to edit config.sh:"
-	. UU/myread
+	. ./UU/myread
 	nostick=''
 	case "$ans" in
 	'') ;;
@@ -22619,7 +22619,7 @@
 		;;
 	esac
 	rp="Run $make depend now?"
-	. UU/myread
+	. ./UU/myread
 	case "$ans" in
 	y*)
 		$make depend && echo "Now you must run '$make'."

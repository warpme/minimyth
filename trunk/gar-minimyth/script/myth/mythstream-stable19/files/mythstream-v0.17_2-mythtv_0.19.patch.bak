diff -Naur mythstream-0.17_2-old/libs/streambrowser.cpp mythstream-0.17_2-new/libs/streambrowser.cpp
--- mythstream-0.17_2-old/libs/streambrowser.cpp	2006-10-10 13:07:33.000000000 -0700
+++ mythstream-0.17_2-new/libs/streambrowser.cpp	2006-10-13 15:37:42.000000000 -0700
@@ -86,10 +86,6 @@
         handled = true;
         close();
       break;
-      case Key_Escape:
-        handled = true;
-        close();
-      break;
    }
 
    if (handled && parentWidget)
diff -Naur mythstream-0.17_2-old/mythstream/main.cpp mythstream-0.17_2-new/mythstream/main.cpp
--- mythstream-0.17_2-old/mythstream/main.cpp	2006-10-08 15:55:35.000000000 -0700
+++ mythstream-0.17_2-new/mythstream/main.cpp	2006-10-13 16:14:40.000000000 -0700
@@ -36,7 +36,7 @@
 #include <mythtv/mythdialogs.h>
 #include <mythtv/mythmedia.h>
 #include <mythtv/mythplugin.h>
-#include <mythtv/libmythui/myththemedmenu.h>
+#include <mythtv/themedmenu.h>
 
 using namespace std;
 
@@ -71,10 +71,12 @@
 }
 
 
-void runMenu(QString themedir, const QString &which_menu)
+void runMenu(QString which_menu)
 {
-    MythThemedMenu *diag = new MythThemedMenu( themedir.ascii(), which_menu,
-					gContext->GetMainWindow()->GetMainStack( ), "stream menu");
+    QString themedir = gContext->GetThemeDir();
+
+    ThemedMenu *diag = new ThemedMenu(themedir.ascii(), which_menu,
+				       gContext->GetMainWindow(), "stream menu");
 
     QString data;
 
@@ -83,7 +85,8 @@
 
     if (diag->foundTheme())
     {
-        GetMythMainWindow()->GetMainStack()->AddScreen(diag);
+        diag->exec();
+        delete diag;
     }
     else
         cerr << "Couldn't find theme " << themedir << endl;
@@ -154,8 +157,7 @@
 
 int mythplugin_config(void)
 {
-    QString themedir = gContext->GetThemeDir( );
-    runMenu( themedir, "streamconfigmenu.xml");
+    runMenu("streamconfigmenu.xml");
     return 0;
 }
 

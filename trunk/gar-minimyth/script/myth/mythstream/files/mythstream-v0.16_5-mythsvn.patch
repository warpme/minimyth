diff -Naur mythstream-0.16_5-old/mythstream/main.cpp mythstream-0.16_5-new/mythstream/main.cpp
--- mythstream-0.16_5-old/mythstream/main.cpp	2005-05-14 06:22:51.000000000 -0700
+++ mythstream-0.16_5-new/mythstream/main.cpp	2006-03-28 09:10:54.000000000 -0800
@@ -35,7 +35,9 @@
 #include <mythtv/mythcontext.h>
 #include <mythtv/mythdialogs.h>
 #include <mythtv/mythplugin.h>
-#include <mythtv/themedmenu.h>
+#include <mythtv/lcddevice.h>
+#include <mythtv/libmythui/myththemedmenu.h>
+
 
 using namespace std;
 
@@ -73,8 +75,8 @@
 {
     QString themedir = gContext->GetThemeDir();
 
-    ThemedMenu *diag = new ThemedMenu(themedir.ascii(), which_menu,
-                                      gContext->GetMainWindow(), "stream menu");
+    MythThemedMenu *diag = new MythThemedMenu(themedir.ascii(), which_menu,
+                                      GetMythMainWindow()->GetMainStack(), "stream menu");
 
     QString data;
 
@@ -83,11 +85,17 @@
 
     if (diag->foundTheme())
     {
-        diag->exec();
-        delete diag;
+        if (class LCD * lcd = LCD::Get())
+        {
+            lcd->switchToTime();
+        }
+        GetMythMainWindow()->GetMainStack()->AddScreen(diag);
     }
     else
+    {
         cerr << "Couldn't find theme " << themedir << endl;
+	delete diag;
+    }
 
 }
 

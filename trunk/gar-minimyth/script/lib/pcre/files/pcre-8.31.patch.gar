diff -Naur pcre-8.31-old/pcre-config.in pcre-8.31-new/pcre-config.in
--- pcre-8.31-old/pcre-config.in	2012-01-21 08:00:28.000000000 -0800
+++ pcre-8.31-new/pcre-config.in	2012-07-06 16:50:56.000000000 -0700
@@ -31,16 +31,16 @@
 libR=
 case `uname -s` in
   *SunOS*)
-  libR=" -R@libdir@"
+  libR=" -R@GAR_DESTDIR@@libdir@"
   ;;
   *BSD*)
-  libR=" -Wl,-R@libdir@"
+  libR=" -Wl,-R@GAR_DESTDIR@@libdir@"
   ;;
 esac
 
 libS=
-if test @libdir@ != /usr/lib ; then
-  libS=-L@libdir@
+if test @GAR_DESTDIR@@libdir@ != @GAR_DESTDIR@/usr/lib ; then
+  libS=-L@GAR_DESTDIR@@libdir@
 fi
 
 while test $# -gt 0; do
@@ -70,15 +70,15 @@
       echo @PACKAGE_VERSION@
       ;;
     --cflags)
-      if test @includedir@ != /usr/include ; then
-        includes=-I@includedir@
+      if test @GAR_DESTDIR@@includedir@ != @GAR_DESTDIR@/usr/include ; then
+        includes=-I@GAR_DESTDIR@@includedir@
       fi
       echo $includes @PCRE_STATIC_CFLAG@
       ;;
     --cflags-posix)
       if test @enable_pcre8@ = yes ; then
-        if test @includedir@ != /usr/include ; then
-          includes=-I@includedir@
+        if test @GAR_DESTDIR@@includedir@ != @GAR_DESTDIR@/usr/include ; then
+          includes=-I@GAR_DESTDIR@@includedir@
         fi
         echo $includes @PCRE_STATIC_CFLAG@
       else

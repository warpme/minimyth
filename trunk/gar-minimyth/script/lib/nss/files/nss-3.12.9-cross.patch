diff -Naur nss-3.12.9-old/mozilla/security/nss/cmd/shlibsign/sign.sh nss-3.12.9-new/mozilla/security/nss/cmd/shlibsign/sign.sh
--- nss-3.12.9-old/mozilla/security/nss/cmd/shlibsign/sign.sh	2011-01-13 11:04:18.000000000 -0800
+++ nss-3.12.9-new/mozilla/security/nss/cmd/shlibsign/sign.sh	2011-01-13 11:04:55.000000000 -0800
@@ -18,8 +18,8 @@
         PATH=${ARG1}/lib:${ARG1}/bin:${ARG4}:${PATH}
     fi
     export PATH
-    echo "${2}"/shlibsign -v -i "${5}"
-    "${2}"/shlibsign -v -i "${5}"
+    echo nss-shlibsign -v -i "${5}"
+    nss-shlibsign -v -i "${5}"
     ;;
 *)
     LIBPATH=`(cd "${1}"/lib; pwd)`:`(cd "${4}"; pwd)`:$LIBPATH
@@ -34,7 +34,7 @@
     export LIBRARY_PATH
     ADDON_PATH=${1}/lib:${4}:$ADDON_PATH
     export ADDON_PATH
-    echo "${2}"/shlibsign -v -i "${5}"
-    "${2}"/shlibsign -v -i "${5}"
+    echo nss-shlibsign -v -i "${5}"
+    nss-shlibsign -v -i "${5}"
     ;;
 esac

diff -Naur zoneinfo-2007h-old/Makefile zoneinfo-2007h-new/Makefile
--- zoneinfo-2007h-old/Makefile	2007-08-20 07:47:41.000000000 -0700
+++ zoneinfo-2007h-new/Makefile	2007-10-05 19:00:30.000000000 -0700
@@ -346,16 +346,14 @@
 
 $(TZLIB):	$(LIBOBJS)
 		-mkdir $(TOPDIR) $(LIBDIR)
-		ar ru $@ $(LIBOBJS)
-		if [ -x /usr/ucb/ranlib -o -x /usr/bin/ranlib ] ; \
-			then ranlib $@ ; fi
+		$(AR) ru $@ $(LIBOBJS)
+		$(RANLIB) $@
 
 # We use the system's logwtmp in preference to ours if available.
 
 date:		$(DATEOBJS)
-		ar r ,lib.a logwtmp.o
-		if [ -x /usr/ucb/ranlib -o -x /usr/bin/ranlib ] ; \
-			then ranlib ,lib.a ; fi
+		$(AR) r ,lib.a logwtmp.o
+		$(RANLIB) ,lib.a
 		$(CC) $(CFLAGS) date.o localtime.o asctime.o strftime.o \
 			$(LDLIBS) -lc ,lib.a -o $@
 		rm -f ,lib.a

diff -Naur zoneinfo-2006m-old/Makefile zoneinfo-2006m-new/Makefile
--- zoneinfo-2006m-old/Makefile	2006-10-02 12:32:16.000000000 -0700
+++ zoneinfo-2006m-new/Makefile	2006-10-05 16:30:31.000000000 -0700
@@ -346,16 +346,14 @@
 
 $(TZLIB):	$(LIBOBJS)
 		-mkdir $(TOPDIR) $(LIBDIR)
-		ar ru $@ $(LIBOBJS)
-		if [ -x /usr/ucb/ranlib -o -x /usr/bin/ranlib ] ; \
-			then ranlib $@ ; fi
+		$(AR) ru $@ $(LIBOBJS)
+		$(RANLIB) $@
 
 # We use the system's logwtmp in preference to ours if available.
 
 date:		$(DATEOBJS)
-		ar r ,lib.a logwtmp.o
-		if [ -x /usr/ucb/ranlib -o -x /usr/bin/ranlib ] ; \
-			then ranlib ,lib.a ; fi
+		$(AR) r ,lib.a logwtmp.o
+		$(RANLIB) ,lib.a
 		$(CC) $(CFLAGS) date.o localtime.o asctime.o strftime.o \
 			$(LDLIBS) -lc ,lib.a -o $@
 		rm -f ,lib.a

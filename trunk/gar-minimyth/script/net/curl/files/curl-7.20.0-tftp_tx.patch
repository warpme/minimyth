diff -Naur curl-7.20.0-old/lib/tftp.c curl-7.20.0-new/lib/tftp.c
--- curl-7.20.0-old/lib/tftp.c	2010-02-04 13:41:46.000000000 -0800
+++ curl-7.20.0-new/lib/tftp.c	2010-02-09 14:54:29.000000000 -0800
@@ -398,11 +398,14 @@
       long tsize = 0;
 
       tsize = strtol( value, NULL, 10 );
-      if(!tsize) {
-        failf(data, "invalid tsize value in OACK packet");
-        return CURLE_TFTP_ILLEGAL;
+      if(state->state == TFTP_STATE_RX)
+      {
+        if(!tsize) {
+          failf(data, "invalid tsize value in OACK packet");
+          return CURLE_TFTP_ILLEGAL;
+        }
+        Curl_pgrsSetDownloadSize(data, tsize);
       }
-      Curl_pgrsSetDownloadSize(data, tsize);
       infof(data, "%s (%ld)\n", "tsize parsed from OACK", tsize);
     }
   }

diff -Naur aewm-1.3.0-old/clients/Makefile aewm-1.3.0-new/clients/Makefile
--- aewm-1.3.0-old/clients/Makefile	2004-11-09 18:30:06.000000000 -0800
+++ aewm-1.3.0-new/clients/Makefile	2005-08-21 11:30:58.000000000 -0700
@@ -16,9 +16,9 @@
 CFGDIR   = $(DESTDIR)/etc/X11/aewm
 
 INCLUDES = -I$(AELIBDIR) -I$(MYLIBDIR)
-X11PATH  = -I$(XROOT)/include
-X11LIBS  = -L$(XROOT)/lib -lX11
-XAWLIBS  = -L$(XROOT)/lib -lX11 -lXt -lXaw
+X11PATH  = -I$(DESTDIR)$(XROOT)/include
+X11LIBS  = -L$(DESTDIR)$(XROOT)/lib -lX11
+XAWLIBS  = -L$(DESTDIR)$(XROOT)/lib -lX11 -lXt -lXaw
 ifdef USE_OLD_GTK
 GTKPATH  = `gtk-config --cflags` -DUSE_OLD_GTK=$(USE_OLD_GTK)
 GTKLIBS  = `gtk-config --libs`
diff -Naur aewm-1.3.0-old/src/Makefile aewm-1.3.0-new/src/Makefile
--- aewm-1.3.0-old/src/Makefile	2004-11-17 12:58:58.000000000 -0800
+++ aewm-1.3.0-new/src/Makefile	2005-08-21 11:30:06.000000000 -0700
@@ -12,13 +12,13 @@
 
 # Uncomment to add MWM hints support. This requires Lesstif headers at
 # build time, but does not link against Lesstif.
-#DEFINES += -DMWM_HINTS
+DEFINES += -DMWM_HINTS
 
 # Uncomment to add freetype support (requires XFree86 4.0.2 or later).
 # This depends on -lXext above, even if you have disabled shape support.
-#DEFINES += -DXFT
-#EXTRA_INC += `pkg-config --cflags xft`
-#EXTRA_LIBS += `pkg-config --libs xft`
+DEFINES += -DXFT
+EXTRA_INC += `pkg-config --cflags xft`
+EXTRA_LIBS += `pkg-config --libs xft`
 
 # Uncomment for debugging info (abandon all hope, ye who enter here).
 #DEFINES += -DDEBUG
@@ -32,8 +32,8 @@
 BINDIR   = $(DESTDIR)$(XROOT)/bin
 MANDIR   = $(DESTDIR)$(XROOT)/man/man1
 CFGDIR   = $(DESTDIR)/etc/X11/aewm
-INCLUDES = -I$(MYLIBDIR) -I$(XROOT)/include $(EXTRA_INC)
-LDPATH   = -L$(XROOT)/lib
+INCLUDES = -I$(MYLIBDIR) -I$(DESTDIR)$(XROOT)/include $(EXTRA_INC)
+LDPATH   = -L$(DESTDIR)$(XROOT)/lib
 LIBS     = -lX11 $(EXTRA_LIBS)
 
 PROG     = aewm

diff -Naur freetype-2.1.9-old/builds/unix/freetype-config.in freetype-2.1.9-new/builds/unix/freetype-config.in
--- freetype-2.1.9-old/builds/unix/freetype-config.in	2004-04-17 11:58:43.000000000 -0700
+++ freetype-2.1.9-new/builds/unix/freetype-config.in	2004-12-14 07:05:57.018128384 -0800
@@ -7,7 +7,6 @@
 libdir=@libdir@
 enable_shared=@enable_shared@
 wl=@wl@
-hardcode_libdir_flag_spec='@hardcode_libdir_flag_spec@'
 
 usage()
 {
@@ -105,9 +104,9 @@
 fi
 
 if test "$echo_cflags" = "yes" ; then
-  cflags="-I$includedir/freetype2"
+  cflags="-I${DESTDIR}/$includedir/freetype2"
   if test "$includedir" != "/usr/include" ; then
-    echo $cflags -I$includedir
+    echo $cflags -I${DESTDIR}/$includedir
   else
     echo $cflags
   fi
@@ -115,12 +114,9 @@
 
 if test "$echo_libs" = "yes" ; then
   rpath=
-  if test "$enable_shared" = "yes" ; then
-    eval "rpath=\"$hardcode_libdir_flag_spec\""
-  fi
   libs="-lfreetype @LIBZ@"
   if test "$libdir" != "/usr/lib" ; then
-    echo -L$libdir $rpath $libs
+    echo -L${DESTDIR}/$libdir $rpath $libs
   else
     echo $libs
   fi
@@ -128,7 +124,7 @@
 
 if test "$echo_libtool" = "yes" ; then
   convlib="libfreetype.la"
-  echo $libdir/$convlib
+  echo ${DESTDIR}/$libdir/$convlib
 fi
 
 # EOF

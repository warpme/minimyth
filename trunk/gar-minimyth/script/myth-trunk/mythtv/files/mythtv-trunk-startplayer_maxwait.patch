diff -Naur mythtv-trunk-16733-old/libs/libmythtv/tv_play.cpp mythtv-trunk-16733-new/libs/libmythtv/tv_play.cpp
--- mythtv-trunk-16733-old/libs/libmythtv/tv_play.cpp	2008-03-20 17:24:22.000000000 -0700
+++ mythtv-trunk-16733-new/libs/libmythtv/tv_play.cpp	2008-03-20 17:24:23.000000000 -0700
@@ -1816,7 +1816,7 @@
     SetupPlayer(isWatchingRecording);
     pthread_create(&decode, NULL, SpawnDecode, nvp);
 
-    maxWait = (maxWait <= 0) ? 20000 : maxWait;
+    maxWait = (maxWait <= 0) ? 40000 : maxWait;
 #ifdef USING_VALGRIND
     maxWait = (1<<30);
 #endif // USING_VALGRIND

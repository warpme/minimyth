diff -Naur mythtv-trunk-18336-old/libs/libmythtv/tv_play.cpp mythtv-trunk-18336-new/libs/libmythtv/tv_play.cpp
--- mythtv-trunk-18336-old/libs/libmythtv/tv_play.cpp	2008-09-19 03:06:29.000000000 -0700
+++ mythtv-trunk-18336-new/libs/libmythtv/tv_play.cpp	2008-09-19 03:06:31.000000000 -0700
@@ -1805,7 +1805,7 @@
     if (pthread_create(&decode, NULL, SpawnDecode, nvp))
         return false;
 
-    maxWait = (maxWait <= 0) ? 20000 : maxWait;
+    maxWait = (maxWait <= 0) ? 40000 : maxWait;
 #ifdef USING_VALGRIND
     maxWait = (1<<30);
 #endif // USING_VALGRIND

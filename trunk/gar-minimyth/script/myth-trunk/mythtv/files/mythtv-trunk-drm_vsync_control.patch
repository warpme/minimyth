diff -Naur mythtv-trunk-24715-old/libs/libmythtv/vsync.cpp mythtv-trunk-24715-new/libs/libmythtv/vsync.cpp
--- mythtv-trunk-24715-old/libs/libmythtv/vsync.cpp	2010-05-17 09:45:58.000000000 -0700
+++ mythtv-trunk-24715-new/libs/libmythtv/vsync.cpp	2010-05-17 09:45:59.000000000 -0700
@@ -80,6 +80,8 @@
 {
     VideoSync *trial = NULL;
     tryingVideoSync  = true;
+    bool tryDRM      = (gCoreContext->GetNumSetting("UseDRMVSync", 1) &&
+                        (getenv("NO_DRM_VSYNC") == NULL));
     bool tryOpenGL   = (gCoreContext->GetNumSetting("UseOpenGLVSync", 1) &&
                         (getenv("NO_OPENGL_VSYNC") == NULL));
 
@@ -99,7 +101,8 @@
 //    TESTVIDEOSYNC(VDPAUVideoSync);
 #endif
 #ifndef _WIN32
-    TESTVIDEOSYNC(DRMVideoSync);
+    if (tryDRM)
+        TESTVIDEOSYNC(DRMVideoSync);
     if (tryOpenGL)
         TESTVIDEOSYNC(OpenGLVideoSync);
 #endif // _WIN32
diff -Naur mythtv-trunk-24715-old/programs/mythfrontend/globalsettings.cpp mythtv-trunk-24715-new/programs/mythfrontend/globalsettings.cpp
--- mythtv-trunk-24715-old/programs/mythfrontend/globalsettings.cpp	2010-05-17 09:45:58.000000000 -0700
+++ mythtv-trunk-24715-new/programs/mythfrontend/globalsettings.cpp	2010-05-17 09:45:59.000000000 -0700
@@ -3774,6 +3774,18 @@
     };
 };
 
+static HostCheckBox *UseDRMVSync()
+{
+    HostCheckBox *gc = new HostCheckBox("UseDRMVSync");
+    gc->setLabel(QObject::tr("Enable DRM vertical sync for timing"));
+    gc->setValue(false);
+    gc->setHelpText(QObject::tr(
+                        "If it is supported by your hardware/drivers, "
+                        "MythTV will use DRM vertical syncing for "
+                        "video timing, reducing frame jitter."));
+    return gc;
+}
+
 #ifdef USING_OPENGL_VSYNC
 static HostCheckBox *UseOpenGLVSync()
 {
@@ -4300,6 +4312,7 @@
     general1->addChild(columns);
     general1->addChild(LiveTVIdleTimeout());
     general1->addChild(AlwaysStreamFiles());
+    general1->addChild(UseDRMVSync());
 #ifdef USING_OPENGL_VSYNC
     general1->addChild(UseOpenGLVSync());
 #endif // USING_OPENGL_VSYNC

diff -Naur mythtv-trunk-17598-old/libs/libmythtv/videoout_xv.cpp mythtv-trunk-17598-new/libs/libmythtv/videoout_xv.cpp
--- mythtv-trunk-17598-old/libs/libmythtv/videoout_xv.cpp	2008-06-23 14:09:01.000000000 -0700
+++ mythtv-trunk-17598-new/libs/libmythtv/videoout_xv.cpp	2008-06-23 16:21:03.000000000 -0700
@@ -1546,7 +1546,12 @@
         for (; it != renderers.end(); ++it)
             tmp += *it + ",";
 
-        renderer = renderers[0];
+        if ((db_vdisp_profile->GetVideoRenderer() == "xvmc-blit") && (renderers.contains("xv-blit")))
+            renderer = "xv-blit";
+        else if ((db_vdisp_profile->GetVideoRenderer() == "xvmc-opengl") && (renderers.contains("opengl")))
+            renderer = "opengl";
+        else
+            renderer = renderers[0];
         VERBOSE(VB_IMPORTANT, LOC + QString(
                     "Desired video renderer '%1' not available.\n\t\t\t"
                     "codec '%2' makes '%3' available, using '%4' instead.")

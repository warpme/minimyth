diff -Naur xf86-video-intel-2.16.0-old/configure.ac xf86-video-intel-2.16.0-new/configure.ac
--- xf86-video-intel-2.16.0-old/configure.ac	2011-08-09 01:34:28.000000000 -0700
+++ xf86-video-intel-2.16.0-new/configure.ac	2011-08-09 14:04:06.000000000 -0700
@@ -58,6 +58,7 @@
 # Initialize libtool
 LT_PREREQ([2.2])
 LT_INIT([disable-static])
+PKG_PROG_PKG_CONFIG
 
 PKG_CHECK_MODULES(GEN4ASM, [intel-gen4asm >= 1.2], [gen4asm=yes], [gen4asm=no])
 AM_CONDITIONAL(HAVE_GEN4ASM, test x$gen4asm = xyes)
@@ -138,7 +139,8 @@
 PKG_CHECK_MODULES(DRI2, [dri2proto >= 2.6])
 PKG_CHECK_MODULES(PCIACCESS, [pciaccess >= 0.10])
 
-sdkdir=`$PKG_CONFIG --variable=sdkdir xorg-server`
+m4_pattern_allow([^PKG_CONFIG_SYSROOT_DIR$])
+sdkdir="$PKG_CONFIG_SYSROOT_DIR$($PKG_CONFIG --variable=sdkdir xorg-server)"
 
 if test "x$enable_dri" != "xno"; then
         save_CFLAGS="$CFLAGS"

diff -Naur mythtv-0.23-26863-old/libs/libmythtv/avformatdecoder.cpp mythtv-0.23-26863-new/libs/libmythtv/avformatdecoder.cpp
--- mythtv-0.23-26863-old/libs/libmythtv/avformatdecoder.cpp	2010-10-18 10:02:16.000000000 -0700
+++ mythtv-0.23-26863-new/libs/libmythtv/avformatdecoder.cpp	2010-10-18 10:02:17.000000000 -0700
@@ -4717,6 +4717,14 @@
 
     GetNVP()->ReinitAudio();
 
+    if (using_passthru != DoPassThrough(codec_ctx))
+    {
+        // detected audio capabilities changed half-way
+        // recheck audio stream
+        VERBOSE(VB_AUDIO, LOC + "Audio config changed. Rescanning");
+        return SetupAudioStream();
+    }
+
     if (LCD *lcd = LCD::Get())
     {
         LCDAudioFormatSet audio_format;

diff -Naur linux-2.6.14.3-old/arch/i386/boot/Makefile linux-2.6.14.3-new/arch/i386/boot/Makefile
--- linux-2.6.14.3-old/arch/i386/boot/Makefile	2005-11-24 14:10:21.000000000 -0800
+++ linux-2.6.14.3-new/arch/i386/boot/Makefile	2005-11-28 21:37:11.000000000 -0800
@@ -19,7 +19,7 @@
 # Set it to -DSVGA_MODE=NORMAL_VGA if you just want the EGA/VGA mode.
 # The number is the same as you would ordinarily press at bootup.
 
-SVGA_MODE := -DSVGA_MODE=NORMAL_VGA
+SVGA_MODE := -DSVGA_MODE=789
 
 # If you want the RAM disk device, define this to be the size in blocks.
 
diff -Naur linux-2.6.14.3-old/arch/x86_64/boot/Makefile linux-2.6.14.3-new/arch/x86_64/boot/Makefile
--- linux-2.6.14.3-old/arch/x86_64/boot/Makefile	2005-11-24 14:10:21.000000000 -0800
+++ linux-2.6.14.3-new/arch/x86_64/boot/Makefile	2005-11-28 21:37:11.000000000 -0800
@@ -19,7 +19,7 @@
 # Set it to -DSVGA_MODE=NORMAL_VGA if you just want the EGA/VGA mode.
 # The number is the same as you would ordinarily press at bootup.
 
-SVGA_MODE := -DSVGA_MODE=NORMAL_VGA
+SVGA_MODE := -DSVGA_MODE=789
 
 # If you want the RAM disk device, define this to be the size in blocks.
 
diff -Naur linux-2.6.14.3-old/kernel/printk.c linux-2.6.14.3-new/kernel/printk.c
--- linux-2.6.14.3-old/kernel/printk.c	2005-11-24 14:10:21.000000000 -0800
+++ linux-2.6.14.3-new/kernel/printk.c	2005-11-28 21:37:03.000000000 -0800
@@ -41,7 +41,7 @@
 
 /* We show everything that is MORE important than this.. */
 #define MINIMUM_CONSOLE_LOGLEVEL 1 /* Minimum loglevel we let people use */
-#define DEFAULT_CONSOLE_LOGLEVEL 7 /* anything MORE serious than KERN_DEBUG */
+#define DEFAULT_CONSOLE_LOGLEVEL 4 /* anything MORE serious than KERN_WARNING */
 
 DECLARE_WAIT_QUEUE_HEAD(log_wait);
 

diff -Naur alsa-driver-1.0.11rc4-old/alsa-kernel/pci/hda/hda_codec.c alsa-driver-1.0.11rc4-new/alsa-kernel/pci/hda/hda_codec.c
--- alsa-driver-1.0.11rc4-old/alsa-kernel/pci/hda/hda_codec.c	2006-03-21 03:24:42.000000000 -0800
+++ alsa-driver-1.0.11rc4-new/alsa-kernel/pci/hda/hda_codec.c	2006-03-23 19:46:46.000000000 -0800
@@ -1905,7 +1905,7 @@
 			snd_hda_codec_setup_stream(codec,
 						   mout->extra_out_nid[i],
 						   stream_tag, 0, format);
-
+#if 0
 	/* surrounds */
 	for (i = 1; i < mout->num_dacs; i++) {
 		if (chs >= (i + 1) * 2) /* independent out */
@@ -1915,6 +1915,7 @@
 			snd_hda_codec_setup_stream(codec, nids[i], stream_tag, 0,
 						   format);
 	}
+#endif
 	return 0;
 }
 
@@ -1926,10 +1927,13 @@
 	hda_nid_t *nids = mout->dac_nids;
 	int i;
 
+#if 0
 	for (i = 0; i < mout->num_dacs; i++)
 		snd_hda_codec_setup_stream(codec, nids[i], 0, 0, 0);
+#endif
 	if (mout->hp_nid)
 		snd_hda_codec_setup_stream(codec, mout->hp_nid, 0, 0, 0);
+ 	snd_hda_codec_setup_stream(codec, HDA_FRONT, 0, 0, 0);
 	for (i = 0; i < ARRAY_SIZE(mout->extra_out_nid); i++)
 		if (mout->extra_out_nid[i])
 			snd_hda_codec_setup_stream(codec,

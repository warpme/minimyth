diff -Naur module-init-tools-3.1-old/Makefile.in module-init-tools-3.1-new/Makefile.in
--- module-init-tools-3.1-old/Makefile.in	2004-11-14 16:59:48.000000000 -0800
+++ module-init-tools-3.1-new/Makefile.in	2005-11-14 07:22:45.000000000 -0800
@@ -67,7 +67,7 @@
 EXEEXT = @EXEEXT@
 OBJEXT = @OBJEXT@
 PATH_SEPARATOR = @PATH_SEPARATOR@
-mandir = $(shell if [ `echo $(prefix)/ | tr -s /` = / ]; then echo /usr/share/man; else echo $(prefix)/man; fi)
+mandir = @mandir@
 INSTALL = $(SHELL) $(top_srcdir)/install-with-care
 AMTAR = @AMTAR@
 AWK = @AWK@
@@ -102,8 +102,8 @@
 EXTRA_depmod_SOURCES = moduleops_core.c
 EXTRA_modinfo_SOURCES = backwards_compat.c
 
-MAN5 = modprobe.conf.5 modules.dep.5
-MAN8 = depmod.8 insmod.8 lsmod.8 rmmod.8 modprobe.8 modinfo.8
+MAN5 =
+MAN8 =
 SGML = $(addprefix doc/,  $(MAN5:%.5=%.sgml) $(MAN8:%.8=%.sgml))
 man_MANS = $(MAN5) $(MAN8)
 

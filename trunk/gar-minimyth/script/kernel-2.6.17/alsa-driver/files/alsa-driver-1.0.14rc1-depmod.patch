diff -Naur alsa-driver-1.0.14rc1-old/Makefile alsa-driver-1.0.14rc1-new/Makefile
--- alsa-driver-1.0.14rc1-old/Makefile	2006-12-07 06:07:18.000000000 -0800
+++ alsa-driver-1.0.14rc1-new/Makefile	2006-12-09 08:43:29.000000000 -0800
@@ -194,7 +194,7 @@
 endif
 	@for d in $(SUBDIRS); do if ! $(MAKE) -C $$d modules_install; then exit 1; fi; done
 ifeq ($(DESTDIR),)
-	-/sbin/depmod -a $(kaversion) $(SYSTEM_MAP_OPT)
+	-depmod -a $(kaversion) $(SYSTEM_MAP_OPT)
 else
 	@echo "*** This is a \`staged' install using the prefix"
 	@echo "***	\"$(DESTDIR)\"."

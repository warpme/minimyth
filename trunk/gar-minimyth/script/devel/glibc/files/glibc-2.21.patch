diff -Naur glibc-2.21-old/configure glibc-2.21-new/configure
--- glibc-2.21-old/configure	2015-07-01 12:53:02.035000413 -0700
+++ glibc-2.21-new/configure	2015-07-01 13:02:16.623000400 -0700
@@ -4433,11 +4433,11 @@
     path_binutils=`(cd $path_binutils; pwd) | sed 's%/*$%/%'`
     CC="$CC -B$path_binutils"
 fi
-AS=`$CC -print-prog-name=as`
-LD=`$CC -print-prog-name=ld`
-AR=`$CC -print-prog-name=ar`
-
-OBJDUMP=`$CC -print-prog-name=objdump`
+AS=${AS}
+LD=${LD}
+AR=${AR}
+  
+OBJDUMP=${OBJDUMP}
 
 OBJCOPY=`$CC -print-prog-name=objcopy`
 

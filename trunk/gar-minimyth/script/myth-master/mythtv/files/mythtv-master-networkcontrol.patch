diff -Naur mythtv-master-20120304-20f2c45-old/mythtv/programs/mythfrontend/main.cpp mythtv-master-20120304-20f2c45-new/mythtv/programs/mythfrontend/main.cpp
--- mythtv-master-20120304-20f2c45-old/mythtv/programs/mythfrontend/main.cpp	2012-03-05 20:52:40.000000000 -0800
+++ mythtv-master-20120304-20f2c45-new/mythtv/programs/mythfrontend/main.cpp	2012-03-05 20:52:41.000000000 -0800
@@ -1678,6 +1678,18 @@
                 QString("NetworkControl failed to bind to port %1.")
                    .arg(port));
     }
+    else
+    {
+        int port = gCoreContext->GetNumSetting("NetworkControlPort", 6546);
+        networkControl = new NetworkControl();
+        QList<QHostAddress> LocalhostList;
+        LocalhostList.clear();
+        LocalhostList.append(QHostAddress("127.0.0.1"));
+        if (!networkControl->listen(LocalhostList, port))
+            LOG(VB_GENERAL, LOG_ERR,
+                QString("NetworkControl failed to bind to port %1.")
+                   .arg(port));
+    }
 
 #ifdef __linux__
 #ifdef CONFIG_BINDINGS_PYTHON

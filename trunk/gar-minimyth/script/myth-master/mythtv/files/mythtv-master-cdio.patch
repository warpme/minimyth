diff -Naur mythtv-master-20110302-624c23c-ecaeffc-old/mythplugins/configure mythtv-master-20110302-624c23c-ecaeffc-new/mythplugins/configure
--- mythtv-master-20110302-624c23c-ecaeffc-old/mythplugins/configure	2011-03-03 07:48:14.000000000 -0800
+++ mythtv-master-20110302-624c23c-ecaeffc-new/mythplugins/configure	2011-03-03 07:48:33.000000000 -0800
@@ -532,6 +532,15 @@
     fi
 
     paranoia="no"
+    cdio="no"
+    if test "$paranoia" = "no" ; then
+        if has_library libcdio_paranoia && has_library libcdio_cdda ; then
+            if has_header cdio/paranoia.h && has_header cdio/cdda.h ; then
+                paranoia="yes"
+                cdio="yes"
+            fi
+        fi
+    fi
     if has_library libcdda_paranoia && has_library libcdda_interface ; then
         if has_header cdda_paranoia.h ; then
             paranoia="yes"
@@ -809,6 +818,14 @@
       echo "#undef  HAVE_STDINT_H" >> ./mythmusic/mythmusic/config.h
     fi
 
+    if test "$cdio" = "yes" ; then
+        echo "#define HAVE_CDIO 1" >> ./mythmusic/mythmusic/config.h
+        echo "LIBS += -lcdio_paranoia -lcdio_cdda"      >> ./mythmusic/mythmusic/config.pro
+    else
+        echo "#undef  HAVE_CDIO"   >> ./mythmusic/mythmusic/config.h
+        echo "LIBS += -lcdda_paranoia -lcdda_interface" >> ./mythmusic/mythmusic/config.pro
+    fi
+
     if test "$cdaudio" = "yes" ; then
       echo "#define HAVE_CDAUDIO 1" >> ./mythmusic/mythmusic/config.h
       echo "CONFIG += cdaudio" >> ./mythmusic/mythmusic/config.pro
diff -Naur mythtv-master-20110302-624c23c-ecaeffc-old/mythplugins/mythmusic/mythmusic/cddecoder.cpp mythtv-master-20110302-624c23c-ecaeffc-new/mythplugins/mythmusic/mythmusic/cddecoder.cpp
--- mythtv-master-20110302-624c23c-ecaeffc-old/mythplugins/mythmusic/mythmusic/cddecoder.cpp	2011-03-03 07:48:15.000000000 -0800
+++ mythtv-master-20110302-624c23c-ecaeffc-new/mythplugins/mythmusic/mythmusic/cddecoder.cpp	2011-03-03 07:48:33.000000000 -0800
@@ -179,7 +179,11 @@
     setOutput(0);
 }
 
+#ifdef HAVE_CDIO
+static void paranoia_cb(long int inpos, paranoia_cb_mode_t function)
+#else
 static void paranoia_cb(long inpos, int function)
+#endif
 {
     inpos = inpos; function = function;
 }
diff -Naur mythtv-master-20110302-624c23c-ecaeffc-old/mythplugins/mythmusic/mythmusic/cddecoder.h mythtv-master-20110302-624c23c-ecaeffc-new/mythplugins/mythmusic/mythmusic/cddecoder.h
--- mythtv-master-20110302-624c23c-ecaeffc-old/mythplugins/mythmusic/mythmusic/cddecoder.h	2011-03-03 07:48:15.000000000 -0800
+++ mythtv-master-20110302-624c23c-ecaeffc-new/mythplugins/mythmusic/mythmusic/cddecoder.h	2011-03-03 07:48:33.000000000 -0800
@@ -12,11 +12,22 @@
 #if defined(__linux__) || defined(__FreeBSD__)
 #include <cdaudio.h>
 extern "C" {
-#include <cdda_interface.h>
-#include <cdda_paranoia.h>
+#ifdef HAVE_CDIO
+#  include <cdio/cdda.h>
+#  include <cdio/paranoia.h>
+#else
+#  include <cdda_interface.h>
+#  include <cdda_paranoia.h>
+#endif
 }
 #endif
 
+#ifdef HAVE_CDIO
+#  define CD_FRAMESIZE_RAW CDIO_CD_FRAMESIZE_RAW
+#  define cdrom_drive cdrom_drive_t
+#  define cdrom_paranoia cdrom_paranoia_t
+#endif
+
 class Metadata;
 
 class CdDecoder : public Decoder
diff -Naur mythtv-master-20110302-624c23c-ecaeffc-old/mythplugins/mythmusic/mythmusic/cdrip.cpp mythtv-master-20110302-624c23c-ecaeffc-new/mythplugins/mythmusic/mythmusic/cdrip.cpp
--- mythtv-master-20110302-624c23c-ecaeffc-old/mythplugins/mythmusic/mythmusic/cdrip.cpp	2011-03-03 07:48:15.000000000 -0800
+++ mythtv-master-20110302-624c23c-ecaeffc-new/mythplugins/mythmusic/mythmusic/cdrip.cpp	2011-03-03 07:48:33.000000000 -0800
@@ -1,3 +1,5 @@
+#include "config.h"
+
 // ANSI C includes
 #include <cstdio>
 #include <cstring>
@@ -11,8 +13,13 @@
 #ifdef HAVE_CDAUDIO
 #include <cdaudio.h>
 extern "C" {
-#include <cdda_interface.h>
-#include <cdda_paranoia.h>
+#ifdef HAVE_CDIO
+#  include <cdio/cdda.h>
+#  include <cdio/paranoia.h>
+#else
+#  include <cdda_interface.h>
+#  include <cdda_paranoia.h>
+#endif
 }
 #endif
 
@@ -138,7 +145,11 @@
     return 0;
 }
 
+#ifdef HAVE_CDIO
+static void paranoia_cb(long int inpos, paranoia_cb_mode_t function)
+#else
 static void paranoia_cb(long inpos, int function)
+#endif
 {
     inpos = inpos; function = function;
 }
@@ -970,6 +981,11 @@
     return token;
 }
 
+#ifdef HAVE_CDIO
+#  define CD_FRAMESIZE_RAW CDIO_CD_FRAMESIZE_RAW
+#  define cdrom_drive cdrom_drive_t
+#  define cdrom_paranoia cdrom_paranoia_t
+#endif
 
 bool Ripper::somethingWasRipped()
 {
diff -Naur mythtv-master-20110302-624c23c-ecaeffc-old/mythplugins/mythmusic/mythmusic/mythmusic.pro mythtv-master-20110302-624c23c-ecaeffc-new/mythplugins/mythmusic/mythmusic/mythmusic.pro
--- mythtv-master-20110302-624c23c-ecaeffc-old/mythplugins/mythmusic/mythmusic/mythmusic.pro	2011-03-03 07:48:15.000000000 -0800
+++ mythtv-master-20110302-624c23c-ecaeffc-new/mythplugins/mythmusic/mythmusic/mythmusic.pro	2011-03-03 07:48:33.000000000 -0800
@@ -23,7 +23,6 @@
 LIBS += -ltag -logg -lvorbisfile -lvorbis -lvorbisenc -lFLAC -lmp3lame
 
 cdaudio: LIBS += -lcdaudio
-paranoia:LIBS += -lcdda_paranoia -lcdda_interface
 
 # Input
 HEADERS += cddecoder.h cdrip.h constants.h databasebox.h

diff -Naur mythtv-0.26-20130117-8f8274a-old/mythtv/programs/mythfrontend/main.cpp mythtv-0.26-20130117-8f8274a-new/mythtv/programs/mythfrontend/main.cpp
--- mythtv-0.26-20130117-8f8274a-old/mythtv/programs/mythfrontend/main.cpp	2013-01-19 05:27:15.000000000 -0800
+++ mythtv-0.26-20130117-8f8274a-new/mythtv/programs/mythfrontend/main.cpp	2013-01-19 05:27:20.000000000 -0800
@@ -1675,6 +1675,18 @@
                 QString("NetworkControl failed to bind to port %1.")
                    .arg(port));
     }
+    else
+    {
+        int port = gCoreContext->GetNumSetting("NetworkControlPort", 6546);
+        networkControl = new NetworkControl();
+        QList<QHostAddress> LocalhostList;
+        LocalhostList.clear();
+        LocalhostList.append(QHostAddress("127.0.0.1"));
+        if (!networkControl->listen(LocalhostList, port))
+            LOG(VB_GENERAL, LOG_ERR,
+                QString("NetworkControl failed to bind to port %1.")
+                   .arg(port));
+    }
 
 #ifdef __linux__
 #ifdef CONFIG_BINDINGS_PYTHON

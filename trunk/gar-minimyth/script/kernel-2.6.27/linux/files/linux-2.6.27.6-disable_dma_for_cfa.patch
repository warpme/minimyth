diff -Naur linux-2.6.27.6-old/drivers/ata/libata-core.c linux-2.6.27.6-new/drivers/ata/libata-core.c
--- linux-2.6.27.6-old/drivers/ata/libata-core.c	2008-11-13 17:16:05.000000000 -0800
+++ linux-2.6.27.6-new/drivers/ata/libata-core.c	2008-11-13 17:16:54.000000000 -0800
@@ -4172,6 +4172,12 @@
 			       "other device, disabling DMA\n");
 	}
 
+	if (ata_id_is_cfa(dev->id)) {
+		xfer_mask &= ~(ATA_MASK_MWDMA | ATA_MASK_UDMA);
+		ata_dev_printk(dev, KERN_WARNING,
+			       "device is a CFA, disabling DMA\n");
+	}
+
 	if (ap->flags & ATA_FLAG_NO_IORDY)
 		xfer_mask &= ata_pio_mask_no_iordy(dev);
 
diff -Naur linux-2.6.27.6-old/drivers/ide/ide-dma.c linux-2.6.27.6-new/drivers/ide/ide-dma.c
--- linux-2.6.27.6-old/drivers/ide/ide-dma.c	2008-11-13 17:16:05.000000000 -0800
+++ linux-2.6.27.6-new/drivers/ide/ide-dma.c	2008-11-13 17:16:54.000000000 -0800
@@ -599,6 +599,12 @@
 				    drive->name, id->model);
 		return blacklist;
 	}
+	if (id->config == 0x848a) {
+		printk(KERN_WARNING "%s: Disabling (U)DMA for %s (CFA)\n",
+				    drive->name, id->model);
+		return 1;
+	}
+
 	return 0;
 }
 

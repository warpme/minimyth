diff -Naur zsnes_1_42-old/src/acinclude.m4 zsnes_1_42-new/src/acinclude.m4
--- zsnes_1_42-old/src/acinclude.m4	2003-11-02 20:05:52.000000000 -0800
+++ zsnes_1_42-new/src/acinclude.m4	2006-12-14 21:02:41.000000000 -0800
@@ -24,7 +24,7 @@
 ZLIB_LIBS="$ZLIB_LIBS -lz"
 LIBS="$LIBS $ZLIB_LIBS"
 CFLAGS="$CFLAGS $ZLIB_CFLAGS"
-with_zlib=no
+with_zlib=yes
 AC_TRY_RUN([
 #include <zlib.h>
 #include <stdio.h>
@@ -85,7 +85,7 @@
       return 1;
     }
 }
-],with_zlib=yes,,[AC_MSG_WARN(Cross Compiling, Assuming zlib is avalible)])
+],with_zlib=yes,with_zlib=no,[AC_MSG_WARN(Cross Compiling, Assuming zlib is avalible)])
 
 if test x$with_zlib = xyes; then
 	AC_MSG_RESULT(yes)
@@ -146,7 +146,7 @@
 CFLAGS="$CFLAGS $LIBPNG_CFLAGS"
 
 AC_MSG_CHECKING(for libpng - version >= $min_libpng_version)
-with_libpng=no
+with_libpng=yes
 
 dnl <--- disable for no user choice part #2
 if test x$enable_libpng != xno; then
@@ -212,7 +212,7 @@
       return 1;
     }
 }
-],with_libpng=yes,,[AC_MSG_WARN(Cross Compiling, Assuming libpng is avalible)])
+],with_libpng=yes,with_libpng=no,[AC_MSG_WARN(Cross Compiling, Assuming libpng is avalible)])
 
 if test x$with_libpng = xyes; then
 	AC_MSG_RESULT(yes)
diff -Naur zsnes_1_42-old/src/configure.in zsnes_1_42-new/src/configure.in
--- zsnes_1_42-old/src/configure.in	2005-01-19 14:52:38.000000000 -0800
+++ zsnes_1_42-new/src/configure.in	2006-12-14 21:01:40.000000000 -0800
@@ -9,13 +9,13 @@
 AC_ARG_WITH(nasm-prefix,[  --with-nasm-prefix=PFX Prefix where nasm is installed (optional)], nasm_prefix="$withval", nasm_prefix="")
 
 dnl -- Where is our compiler, and who are we compiling for?
-CFLAGS="$CFLAGS -pipe -I. -Wall -I/usr/local/include -I/usr/include"
+CFLAGS="$CFLAGS -pipe -I."
 
 dnl -- You might wanna change -I/usr/include if you're trying to do this with
 dnl Mingw, because it doesnt have a standard file tree. Maybe if ming do this
 dnl else do -I/usr/include. Or some shit. You figure it out.
 
-LDFLAGS="$LDFLAGS -L/usr/local/lib -L/usr/lib"
+LDFLAGS="$LDFLAGS"
 
 dnl -- Same thing here.
 

################################################################################
# ld.mk
################################################################################

ld: /etc
	ldconfig -f /etc/ld.so.conf -C /etc/ld.so.cache
	touch ld

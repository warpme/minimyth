diff -Naur mythtv-0.21-25121-old/bindings/python/python.pro mythtv-0.21-25121-new/bindings/python/python.pro
--- mythtv-0.21-25121-old/bindings/python/python.pro	2010-06-20 07:18:32.000000000 -0700
+++ mythtv-0.21-25121-new/bindings/python/python.pro	2010-06-28 10:12:00.000000000 -0700
@@ -1,14 +1,22 @@
 include ( ../../config.mak )
 
-python_install.target = install
-python_install.depends = all
+python_build.target = python_build
+python_build.depends = setup.py FORCE
+python_build.commands = python setup.py build --cross-compile
 
-contains(PREFIX, ^/usr(/local)?/?$) {
-python_install.commands = python setup.py install --root="$(INSTALL_ROOT)"
-} else {
-python_install.commands = python setup.py install --root="$(INSTALL_ROOT)" --prefix="$${PREFIX}"
+python_install.target = python_install
+python_install.CONFIG = no_path
+python_install.commands = python setup.py install --skip-build --prefix="$(if $(INSTALL_ROOT),$(INSTALL_ROOT),/)$${PREFIX}"
+
+# This is done so the default /usr/local prefix installs to the "normal"
+# /usr/lib/python{VER}/site-packages directory.
+!contains(PREFIX, ^/usr(/local)?/?$) {
+	python_install.commands += --prefix=\"$${PREFIX}\"
 }
 
-QMAKE_LINK=@-echo
-QMAKE_EXTRA_UNIX_TARGETS += python_install
+QMAKE_CLEAN += dummy_file; python setup.py clean --all
 
+PRE_TARGETDEPS += python_build
+QMAKE_LINK=@-echo
+QMAKE_EXTRA_UNIX_TARGETS += python_build
+INSTALLS += python_install
diff -Naur mythtv-0.21-25121-old/bindings/python/setup.py mythtv-0.21-25121-new/bindings/python/setup.py
--- mythtv-0.21-25121-old/bindings/python/setup.py	2010-06-20 07:18:32.000000000 -0700
+++ mythtv-0.21-25121-new/bindings/python/setup.py	2010-06-28 10:12:00.000000000 -0700
@@ -1,6 +1,6 @@
 #!/usr/bin/python
 
-from distutils.core import setup
+from setuptools import setup
 
 setup(
 		name='MythTV',

diff -Naur linux-2.6.20.6-old/arch/i386/boot/Makefile linux-2.6.20.6-new/arch/i386/boot/Makefile
--- linux-2.6.20.6-old/arch/i386/boot/Makefile	2007-04-08 08:59:57.000000000 -0700
+++ linux-2.6.20.6-new/arch/i386/boot/Makefile	2007-04-08 09:01:24.000000000 -0700
@@ -19,7 +19,7 @@
 # Set it to -DSVGA_MODE=NORMAL_VGA if you just want the EGA/VGA mode.
 # The number is the same as you would ordinarily press at bootup.
 
-SVGA_MODE := -DSVGA_MODE=NORMAL_VGA
+SVGA_MODE := -DSVGA_MODE=786
 
 # If you want the RAM disk device, define this to be the size in blocks.
 
diff -Naur linux-2.6.20.6-old/arch/x86_64/boot/Makefile linux-2.6.20.6-new/arch/x86_64/boot/Makefile
--- linux-2.6.20.6-old/arch/x86_64/boot/Makefile	2007-04-08 08:59:57.000000000 -0700
+++ linux-2.6.20.6-new/arch/x86_64/boot/Makefile	2007-04-08 09:01:24.000000000 -0700
@@ -19,7 +19,7 @@
 # Set it to -DSVGA_MODE=NORMAL_VGA if you just want the EGA/VGA mode.
 # The number is the same as you would ordinarily press at bootup.
 
-SVGA_MODE := -DSVGA_MODE=NORMAL_VGA
+SVGA_MODE := -DSVGA_MODE=786
 
 # If you want the RAM disk device, define this to be the size in blocks.
 
diff -Naur linux-2.6.20.6-old/kernel/printk.c linux-2.6.20.6-new/kernel/printk.c
--- linux-2.6.20.6-old/kernel/printk.c	2007-04-08 08:59:57.000000000 -0700
+++ linux-2.6.20.6-new/kernel/printk.c	2007-04-08 09:01:24.000000000 -0700
@@ -42,7 +42,7 @@
 
 /* We show everything that is MORE important than this.. */
 #define MINIMUM_CONSOLE_LOGLEVEL 1 /* Minimum loglevel we let people use */
-#define DEFAULT_CONSOLE_LOGLEVEL 7 /* anything MORE serious than KERN_DEBUG */
+#define DEFAULT_CONSOLE_LOGLEVEL 4 /* anything MORE serious than KERN_WARNING */
 
 DECLARE_WAIT_QUEUE_HEAD(log_wait);
 

diff -Naur alsa-driver-1.0.14rc4-old/Makefile alsa-driver-1.0.14rc4-new/Makefile
--- alsa-driver-1.0.14rc4-old/Makefile	2007-05-02 01:02:19.000000000 -0700
+++ alsa-driver-1.0.14rc4-new/Makefile	2007-05-03 13:41:48.000000000 -0700
@@ -198,7 +198,7 @@
 endif
 	@for d in $(SUBDIRS); do if ! $(MAKE) -C $$d modules_install; then exit 1; fi; done
 ifeq ($(DESTDIR),)
-	-/sbin/depmod -a $(kaversion) $(SYSTEM_MAP_OPT)
+	-depmod -a $(kaversion) $(SYSTEM_MAP_OPT)
 else
 	@echo "*** This is a \`staged' install using the prefix"
 	@echo "***	\"$(DESTDIR)\"."
